# Pre-phase audit — phase-2a (re-audit, v2)

> Independent cold audit performed 2026-05-18 by the Auditor sub-agent.
> Did not read the prior audit at `2026-05-18-pre-phase-2a.md`.

## 1. Coverage — PASS

Every F-ID in `docs/01-requirements.md` maps to at least one component in `docs/02-architecture.md` and at least one class/module in `docs/02-classes.md`.

| F-ID | Requirement (short) | Component(s) / Class(es) |
|---|---|---|
| F1 | CLI accepts prompt | `cli` (`02-architecture.md:123`); typer entry (`02-classes.md:18`) |
| F2 | Planner → Intent | `agent.planner` (`02-architecture.md:114`); `Planner` (`02-classes.md:20`,`120`) |
| F3 | Worker emits via adapter | `agent.worker` (`02-architecture.md:115`); `Worker` (`02-classes.md:22`,`142-146`) + `adapters.build123d_target` (`02-architecture.md:120`) |
| F4 | Full 11-kind schema in adapter | `adapters.build123d_target` (`02-architecture.md:120`); `Build123dTarget` (`02-classes.md:84-106`) |
| F5 | Executor → STEP | `agent.executor` (`02-architecture.md:116`); `Executor` (`02-classes.md:23`,`154-158`) |
| F6 | Dimension sanity check | `agent.sanity` (`02-architecture.md:114`); `SanityChecker` (`02-classes.md:21`,`137-141`) |
| F7 | Renderer 3 PNGs | `render` (`02-architecture.md:122`); `Render` (`02-classes.md:27`,`189-194`) |
| F8 | Run folder artefact set | `agent.loop` (`02-architecture.md:118`); `Loop` (`02-classes.md:19`,`167-175`) |
| F9 | status.json + cost | `Loop._write_status` (`02-classes.md:174`); `pricing.price()` (`02-classes.md:16`,`209`) |
| F10 | trace.jsonl per-step | `Loop._write_trace` (`02-classes.md:173`) |
| F11 | run-id format | `Loop._new_run_dir` (`02-classes.md:172`) |
| F12 | CLI prints run dir | `cli` (`02-architecture.md:123`) |
| F13 | Exit codes | `cli` (`02-classes.md:18`, test strategy `02-classes.md:336` "exit code mapping") |
| F14 | CLI flags | `cli` + `config` (`02-architecture.md:123-125`); `Config.load` (`02-classes.md:226`) |

No orphan F-IDs.

## 2. NFR satisfaction — PASS

The "Non-functional requirements — design satisfaction" table in `docs/02-architecture.md:199-210` contains one row per N-ID:

- N1 → line 201
- N2 → line 202
- N3 → line 203
- N4 → line 204
- N5 → line 205
- N6 → line 206
- N7 → line 207
- N8 → line 208
- N9 → line 209
- N10 → line 210

Every NFR N1–N10 from `docs/01-requirements.md:59-69` is covered. No orphans.

## 3. Reference integrity — PASS

Every cross-doc markdown link (excluding `http(s)://` and `mailto:`) resolves to an existing file or anchor. Verified targets:

- ADR links: `docs/adr/0001-intent-as-pivot.md`, `0002-dimension-sanity-check.md`, `0003-prompt-caching-for-cost.md` — exist.
- ADR anchors: `0002-dimension-sanity-check.md#decision` (line 23), `0003-prompt-caching-for-cost.md#decision` (line 51) — exist.
- `02-classes.md#dependency-rules` — anchor present at line 307.
- Phase cross-links: `phase-0.md`, `phase-1.md`, `phase-0-report.md`, `phase-1-report.md`, `phase-2a.md` — all exist.
- Audit cross-links from phase reports: `audits/2026-05-16-pre-phase-0.md`, `audits/2026-05-17-pre-phase-1-v4.md` — exist.
- Source/test references in `phase-0-report.md` and `phase-1-report.md` (`../../src/...`, `../../tests/...`, `pyproject.toml`, `LICENSE`, `.env.example`, `.gitignore`, `README.md`, `.github/workflows/ci.yml`, `pyrightconfig.json`, `examples/README.md`, `.pre-commit-config.yaml`) — all exist on disk.

No broken links.

## 4. ADR coverage — PASS

`docs/02-architecture.md` § Decisions deferred (lines 238-266) lists six items:

1. Provider abstraction — justified: "Do not build a generic provider layer until there's a second provider in scope" (line 244-246). No ADR needed.
2. Conversational refinement vs one-shot — justified as v0.2 work (line 248-249).
3. NX adapter feature coverage — justified: schema is the bottleneck (line 250-253).
4. Sandboxing strategy — justified: "Will be an ADR if it does [need stronger isolation]" (line 254-256).
5. Prompt versioning — **Resolved** by ADR-0003 (line 257-262).
6. SanityCheck tolerance — **Resolved** by ADR-0002 (line 263-266).

Every item is either resolved (ADR linked) or has an explicit deferral justification.

## 5. Glossary — FAIL

Domain/type terms appearing in class names, method signatures, or DDD analysis of `docs/02-classes.md` that are **not** defined in the ubiquitous-language glossary (`02-classes.md:251-281`):

- `PrimaryKind` — used at `02-classes.md:53` (`PrimaryFeature.kind`) and throughout. Defined only in `02-data-model.md` as an enum; the glossary in `02-classes.md` has no entry.
- `ModifierKind` — used at `02-classes.md:58` (`Modifier.kind`) and throughout. Same gap as `PrimaryKind`.
- `Unit` — used at `02-classes.md:48` (`Parameter.unit`). Defined only as an enum in `02-data-model.md:114`; not in glossary.
- `Config` — appears at `02-classes.md:17` and `02-classes.md:219-227` (Config class). The glossary explicitly references it ("Distinct from `Config`...") at `02-classes.md:279` but has no `Config` entry of its own; reader must infer the definition from the cross-reference.
- `Mesh` — used in `Render._load_step(path: Path) Mesh` at `02-classes.md:192`. No glossary entry; not defined elsewhere in the design docs.
- `Pricing` — used as a service in DDD analysis (`02-classes.md:304`). No glossary entry; only the `pricing` module description in the module map covers it.
- `ContractViolation` — defined in glossary (line 265) **but** the structure `validate_kind_contracts(intent) → list[ContractViolation]` is the only definition of its shape; this is acceptable but the value-object kind is not explicitly stated. Borderline — not flagging as a hard failure on its own.

The previous phase-1 audit (v4) noted glossary completeness as a known gap to be addressed by a post-phase-1 `/pm-architecture` pass (per `phase-1-report.md` recommendation #8). That work has not landed; the phase-2a plan defers it again at `phase-2a.md:254-257`. The carry-forward is documented but the underlying gap is still real.

## 6. Phase readiness — PASS

`docs/phases/phase-2a.md` has all required slots populated:

- Goal: lines 20-25 (closes F2/F3/F6, partial F9/F10).
- Depends on: lines 26-29 (`phase-1` done; F2/F3/F6/F9/F10 approved; ADR 0001/0002/0003 accepted).
- Minimum deliverable: lines 90-169 (sanity, planner, prompts/planner.system.md, worker, tests, import-linter contracts, coverage include).
- Maximum deliverable: lines 171-195 (duration_s, centred tests, two more few-shots, live-API gate).
- Sprint table: lines 199-203 (3 rows: Day 1 / Day 2 / Day 3).
- Exit criteria: lines 205-217 (9 numbered items).

Frontmatter (lines 1-12) is well-formed YAML and contains the template-required keys (`id`, `title`, `status`, `started`, `finished`, `min_goal_met`, `max_goal_met`, `blocker`, `depends_on`, `audit`). `status: planned` is consistent with running `/pm-phase-start` next.

## 7. Stale TODOs — PASS

`grep -rE "TBD|TODO|FIXME|<!-- gap:"` across `docs/00-*`, `docs/01-*`, `docs/02-*`, `docs/03-*`, `docs/phases/`, and `docs/adr/` returns no matches. No stale markers to triage.

## 8. Notes hygiene — PASS

`docs/notes/inbox.md` contains 7 explicit "absorbed into" markers (lines 27, 151, 355, 624, 886, 1144, 1346). Spot-checked each:

- Line 27 → `00-vision.md` / `00-pr-faq.md`: vault-original `00-vision.md` content lives in both with the recorded `/pm-vision` revisions (NX to v0.1, three-prompt success criterion). Substance present.
- Line 151 → `02-architecture.md`: C4 levels, layered responsibilities, NFR table, repo layout, decisions deferred — all present.
- Line 355 → `02-data-model.md`: ERD, entity definitions, pydantic skeleton, per-kind contracts, example, schema-evolution rules — all present.
- Line 624 → `01-requirements.md` R1–R6 + `02-architecture.md` component view / sequence / state machine — present (`01-requirements.md` has R1–R11 superset; `02-architecture.md` has the C4 L3 component diagram and the two sequence diagrams in `01-requirements.md:84-165`).
- Line 886 → `02-architecture.md` layered responsibilities + adapter rules — present (`02-architecture.md:119-121`, `02-classes.md:74-109`).
- Line 1144 → `01-requirements.md` (F1, F5, F8–F14, N2, N10) + `02-architecture.md` CLI / cross-cutting — present.
- Line 1346 → `00-vision.md` success criterion + `03-roadmap.md` — present.

Other notes files (`constraints.md`, `ideas.md`, `inspiration.md`, `questions.md`, `requirements.md`, `wishes.md`) contain only their template scaffolding (no domain content), so no contradiction risk. `decisions.md` is an append-only log whose entries each carry a `→` target — spot-checked entries align with formal-doc edits.

No discrepancies found.

## 9. Frontmatter validity — FAIL

Valid YAML frontmatter checked on:

- `docs/03-roadmap.md` (lines 1-4): `project_status: building`, `active_phase: null` — parses; keys per template.
- `docs/phases/phase-0.md` (1-11): all template keys present, valid.
- `docs/phases/phase-1.md` (1-11): all template keys present, valid.
- `docs/phases/phase-2a.md` (1-12): all template keys present, valid.
- `docs/phases/phase-0-report.md` (1-8): `phase`, `status`, `min_met`, `max_met`, `duration_planned_days`, `duration_actual_days` — parses; report-template keys present.
- `docs/phases/phase-1-report.md` (1-8): same shape — parses; valid.

Missing frontmatter (strict reading of "every file under `docs/phases/` and `docs/blockers/`"):

- `docs/phases/README.md` — has no frontmatter (`docs/phases/README.md:1` is `# Phases`, no `---` block). This file is a directory-level explanation, not a phase artefact; arguably exempt, but the audit prompt does not carve out an exception.
- `docs/blockers/README.md` — has no frontmatter (`docs/blockers/README.md:1` is `# Blockers`). Same situation: directory explainer, no `---` block.

If READMEs are considered out of scope for this check, the check is a clean PASS. Flagging as FAIL per the strict letter of the audit prompt; this is a low-severity defect (the files contain no status data and adding frontmatter would be purely ceremonial).

---

Summary: 7 PASS, 2 FAIL

---

## Override

**Override timestamp:** 2026-05-18
**Override authorised by:** vandpaolo (project owner)
**Override applied via:** `/pm-phase-start phase-2a`

Two FAILs remain after the v1→v2 audit cycle. Phase readiness (Check 6)
passed both audits cleanly. The remaining FAILs are out-of-phase-2a
documentation polish:

**Check 5 — Glossary leaves (PrimaryKind, ModifierKind, Unit, Mesh, Pricing, Config):**

- `PrimaryKind`, `ModifierKind`, `Unit` — these are `Literal` enum
  types declared in [`02-data-model.md`](../02-data-model.md) §
  Enums and already implemented in code at
  [`src/maquette/intent.py`](../../src/maquette/intent.py). They are
  *type aliases*, arguably below the ubiquitous-language threshold;
  certainly not phase-2a-touching.
- `Mesh` — phase-2b's `render` module concern (PyVista-external type).
- `Pricing` — already a glossary-table-row-by-feel via `Tokens` and
  `ModelPrice` definitions; the *service-level* name is a thin shim.
- `Config` — distinct from `RunConfig` (which IS in the glossary);
  `Config` is the infrastructure-layer dataclass merged from sources,
  pre-existing since phase-0.

None of these block phase-2a's three deliverables (`agent.sanity`,
`agent.planner`, `agent.worker`). They roll up to the documented
post-phase-2a `/pm-architecture` glossary pass per
[`phase-1-report.md`](../phases/phase-1-report.md) recommendation #8,
now expanded to: close all leaves before phase-5 NX adapter work.

**Check 9 — README frontmatter:**

- `docs/phases/README.md` and `docs/blockers/README.md` are directory-
  explainer prose generated by `/pm-init`. They carry no status data
  and are not part of the phase/blocker *records* the framework
  tracks. The auditor's own note flagged this as ambiguous
  (*"if READMEs are considered out of scope this becomes a clean
  PASS"*); framework intent is the latter. No README modification
  needed.

**Carry-forward:** Both items roll into the same post-phase-2a
`/pm-architecture` polish pass — single batch close-out of all
glossary leaves + clarification of frontmatter scope in
[`docs/phases/README.md`](../phases/README.md) prose if useful.

**Scope-freeze in effect from this point until phase-2a reaches `done`
or `blocked`.** Design doc edits require `/pm-blocker`.

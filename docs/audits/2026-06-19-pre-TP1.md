# Pre-phase audit — TP1 (Layer Stack backend)

- **Date:** 2026-06-19
- **Target phase:** TP1
- **Auditor:** general-purpose sub-agent (cold read, no conversation context)

---

## 1. Coverage — PASS

Every functional requirement F1–F47 is implemented by at least one named component or class. The six F-IDs flagged as "uncited" (`F8`, `F17`, `F21`, `F26`, `F28`, `F30`) are each in fact realized by a named component — the design simply omits the inline `(F##)` traceability tag:

- F8 (op appends to history → viewport updates): `document` (`02-architecture.md:183`) + `doc-store`/`viewport`.
- F17 (cancel in-flight op): `session` cancel token (`02-architecture.md:182`) + Cancellation cross-cutting (`02-architecture.md:381`).
- F21 (structured error, no raw traceback): `intent_validation` component; cited by ID in `02-data-model.md:287,319`.
- F26 (GitHub host + semver): Distribution/Lifecycle context, `02-classes.md:296`.
- F28 (install by downloading `.exe`): `electron-builder` packaging / N4, `02-architecture.md:338`.
- F30 (`out_root` → `/srv/touch/`): `config` component, `02-architecture.md:193`, `02-classes.md:438`.

Minor traceability nit (not a coverage failure): adding `(F8)`, `(F17)`, `(F26)`, `(F28)`, `(F30)` tags to those component rows would make the requirements→design trace mechanically checkable.

## 2. NFR satisfaction — PASS

All 16 NFRs (N1–N16) have a row in the "Non-functional requirements — design satisfaction" table (`docs/02-architecture.md:333–350`). No orphans.

## 3. Reference integrity — FAIL

Six relative links don't resolve. All are in non-design files (notes source-prose / historical audits), none in the active `00/01/02/03/adr` design docs:

- `docs/notes/inbox.md:921` → `./02-intent-schema.md` (missing)
- `docs/notes/inbox.md:1133` → `./02-intent-schema.md` (missing)
- `docs/notes/inbox.md:1271` → `./03-agent-loop.md` (missing)
- `docs/notes/inbox.md:1414` → `./02-intent-schema.md` (missing)
- `docs/audits/2026-05-17-pre-phase-1-v2.md:63` → `./02-classes.md` (wrong relative depth; file is at `docs/02-classes.md`)
- `docs/audits/2026-05-18-pre-phase-2a-v2.md:158` → `../../src/maquette/intent.py` (path is now `src/touch_backend/`)

These are stale links to Maquette-era filenames in frozen historical docs. Low impact, but they are broken `docs/**/*.md` links so the check fails. All anchor links resolve to existing files.

## 4. ADR coverage — PASS

The six "Decisions deferred" entries (`docs/02-architecture.md:389–411`) each carry an explicit justification for remaining deferred (FE framework, binary mesh frame, WS auth, op-history granularity, multi-doc sessions, dev sidecar process model). None requires an ADR yet. The pivot decisions are already promoted to ADRs 0012–0016.

## 5. Glossary — FAIL

Two domain terms used in `docs/02-classes.md` are absent from the ubiquitous-language glossary (`02-classes.md:309–350`):

- **`mesh_cache`** — appears as a load-bearing backend dependency of `layer_stack` (`02-classes.md:31`, listed under "Depends on (out)") and in `02-architecture.md:199,206`, but it has **no row in the backend module map** and **no glossary entry**. It is a TP1 dependency (carried from T4) that the classes doc references without defining. This is the most material gap: TP1's central `layer_stack` module folds through `mesh_cache`, yet `mesh_cache` is undocumented as a module.
- **`1-ring`** — used in the `context_packets` description (`02-classes.md:35`, and `02-architecture.md:210`) for adjacency neighborhood; never defined in the glossary.

Other pivot terms (Layer, Layer Stack, Provenance, MCP server, Context packet, Stack revision, Recognized template / code layer) are all defined.

## 6. Phase readiness — PASS

`docs/phases/phase-TP1.md` has all required slots non-empty: goal (`:15`), depends_on (`:10` frontmatter `[T5]` + `:21` section), minimum deliverable (`:30–38`), maximum deliverable (`:41–43`), sprint table with 10 rows (`:47–58`), exit criteria (`:60–68`). Frontmatter is well-formed.

## 7. Stale TODOs — PASS

No `TBD`/`TODO`/`FIXME`/`<!-- gap:` markers exist in the active design docs. The only matches outside `notes/`+`audits/` are in phase report/plan files, every one a historical reference to a since-resolved `02-classes.md:331` executor-process-model TBD (resolved at `02-classes.md:373`, recorded in `decisions.md` 2026-05-31 P3). No live stale marker.

## 8. Notes hygiene — PASS

All `> absorbed into …` markers in `docs/notes/` point to targets whose substance is present (F18/F32/F33/F34 → requirements; adapter-export → data-model + ADR-0004; inbox markers → vision/pr-faq/requirements/architecture/data-model/ADRs 0005,0006,0009). No sampled unabsorbed section contradicts its target doc.

## 9. Frontmatter validity — PASS

All files under `docs/phases/`, `docs/blockers/`, and `docs/03-roadmap.md` have valid, parseable YAML frontmatter with consistent key sets per template. The only files without frontmatter are `README.md` index files (expected). `docs/03-roadmap.md` has `active_phase: null` — consistent with running a *pre*-phase audit.

---

**Summary: 7 PASS, 2 FAIL**

The two FAILs are low-severity: Check 3 (6 broken links, all in frozen notes/audits with Maquette-era paths) and Check 5 (glossary). The one finding worth fixing before TP1 starts is in Check 5 — **`mesh_cache` is a direct dependency of TP1's `layer_stack` module but has no module-map row or glossary entry in `02-classes.md`**; either add a module row (it was built in T4) or a glossary definition so the TP1 dependency is documented.

---

## Resolution (2026-06-19)

- **Check 5 — RESOLVED** via a targeted `/pm-architecture` touch (scope-freeze was off):
  added a `touch_backend.mesh_cache` row to the backend module map and glossary
  entries for **Mesh cache / `mesh_cache`** and **1-ring** in `docs/02-classes.md`.
  Both terms now resolve; Check 5 would re-run PASS.
- **Check 3 — ACCEPTED as expected history, not fixed.** All 6 broken links live in
  frozen Maquette-era docs (`notes/inbox.md`, two pre-pivot audits) and point at
  superseded filenames. Per the project rule that historical Maquette docs are never
  renamed (preserved history), these are intentional pivot artifacts. No action.

Effective status for greenlight: **8 PASS, 1 accepted-historical FAIL.** TP1 cleared to start.

# Pre-phase-1 audit (v2, independent re-audit)

- Date: 2026-05-17
- Target phase: `phase-1` (Adapter)
- Auditor: independent cold review (PM Framework Auditor sub-agent)
- Scope: nine standard pre-phase checks across `docs/` (excluding the
  prior same-day audit at `docs/audits/2026-05-17-pre-phase-1.md`,
  which was not read).

## 1. Coverage — PASS

Every functional requirement F1–F14 in `docs/01-requirements.md` is
implemented by at least one component / class in `docs/02-architecture.md`
or `docs/02-classes.md`:

- F1 (CLI accepts prompt) → `cli` module (`docs/02-architecture.md:122`,
  `docs/02-classes.md:18`).
- F2 (Planner → validated Intent + 1 retry) → `agent.planner` /
  `Planner.plan` (`docs/02-architecture.md:112`, `docs/02-classes.md:20`,
  `docs/02-classes.md:120`).
- F3 (Worker emits build123d via adapter) → `agent.worker` +
  `adapters.build123d_target` (`docs/02-architecture.md:114`,
  `docs/02-classes.md:22`, `docs/02-classes.md:25`).
- F4 (full v0 schema — 6 PrimaryKinds + 5 ModifierKinds) →
  `adapters.build123d_target.emit` with per-kind helpers
  (`docs/02-classes.md:84-103`).
- F5 (Executor → STEP) → `agent.executor` / `Executor.execute`
  (`docs/02-architecture.md:115`, `docs/02-classes.md:23`,
  `docs/02-classes.md:154`).
- F6 (dimension sanity check) → `agent.sanity` / `SanityChecker.check`
  (`docs/02-architecture.md:113`, `docs/02-classes.md:21`,
  `docs/02-classes.md:137`).
- F7 (3 orthographic PNGs) → `render.orthographic`
  (`docs/02-architecture.md:121`, `docs/02-classes.md:27`).
- F8 (run-folder artefact set) → `agent.loop`
  (`docs/02-architecture.md:117`, `docs/02-classes.md:167`).
- F9 (status.json with cost accounting) → `agent.loop` writers +
  `pricing.price` (`docs/02-classes.md:174`, `docs/02-classes.md:207`).
- F10 (trace.jsonl per-state) → `Loop._write_trace`
  (`docs/02-classes.md:172`).
- F11 (run-id format) → `Loop._new_run_dir`
  (`docs/02-classes.md:171`).
- F12 (CLI prints run dir) → `cli` (`docs/02-architecture.md:122`).
- F13 (exit code table) → `cli` (test strategy: "exit code mapping",
  `docs/02-classes.md:320`).
- F14 (CLI flags) → `cli` + `config.Config`
  (`docs/02-architecture.md:122-124`, `docs/02-classes.md:219`).

No orphan F-IDs.

## 2. NFR satisfaction — PASS

The "Non-functional requirements — design satisfaction" table at
`docs/02-architecture.md:198-209` has one row each for N1–N10. No
orphan N-IDs.

## 3. Reference integrity — FAIL

- `docs/02-architecture.md:75` — `See [02-classes.md § Dependency
  rules].` is a bare markdown reference (`[…]` with no `(…)` URL and
  no matching `[label]: url` definition elsewhere in the file). Renders
  as literal `[02-classes.md § Dependency rules]` rather than a link.
  Should be `[02-classes.md § Dependency rules](./02-classes.md)` (or
  point at the in-file heading via anchor) to actually resolve.

All other intra-`docs/` markdown links resolve to existing files:

- `docs/02-data-model.md:25 → ./adr/0001-intent-as-pivot.md` ✓
- `docs/02-architecture.md:257,263,268-270 → ./adr/000{1,2,3}-*.md` ✓
- `docs/03-roadmap.md:60 → phases/phase-0.md` ✓
- `docs/adr/0001-intent-as-pivot.md:46-47 → ../02-data-model.md,
  ../02-architecture.md, ../02-classes.md` ✓
- `docs/adr/README.md:43-45 → ./000{1,2,3}-*.md` ✓
- `docs/phases/phase-0-report.md:13-24 → phase-0.md,
  ../audits/2026-05-16-pre-phase-0.md, ../../pyproject.toml,
  ../../LICENSE, ../../.gitignore, ../../.env.example, ../../README.md,
  ../../src/maquette/{intent,intent_validation,pricing,config}.py,
  ../../tests/test_{intent,intent_validation,pricing,config}.py,
  ../../.github/workflows/ci.yml, ../../pyrightconfig.json,
  ../../examples/README.md, ../../.pre-commit-config.yaml` — all exist
  on disk.
- `docs/phases/phase-1.md:22,30,44,100,174 → phase-0.md,
  phase-0-report.md, ../02-classes.md` ✓

No anchor-style links (`#heading`) appear anywhere, so anchor
resolution is moot.

## 4. ADR coverage — PASS

Six items appear under "Decisions deferred" at
`docs/02-architecture.md:236-265`:

1. **Provider abstraction** (`02-architecture.md:241-245`) — justified
   as deferred (single-provider Claude in v0; "Do not build a generic
   provider layer until there's a second provider in scope"). Also
   cross-referenced from `00-vision.md § Non-goals` and decisions.md
   2026-05-16 /pm-architecture C1.
2. **Conversational refinement vs one-shot**
   (`02-architecture.md:246-248`) — justified as deferred to v0.2 with
   the shape (REPL vs file vs watched) left open intentionally.
3. **NX adapter feature coverage**
   (`02-architecture.md:249-252`) — justified ("Start with the same
   primitives the build123d adapter supports … Expanding the NX surface
   beyond the Intent schema is explicitly a non-goal"). Matches roadmap
   open decision #5.
4. **Sandboxing strategy** (`02-architecture.md:253-255`) — justified
   ("Subprocess + resource limits for v0. Containerised execution
   (Docker) is on the v0.1+ list … Will be an ADR if it does"). Phase
   7a in `03-roadmap.md:119-123` carries the v0.1 work.
5. **Prompt versioning approach** (`02-architecture.md:256-261`) —
   resolved by ADR-0003.
6. **SanityCheck tolerance** (`02-architecture.md:262-265`) — resolved
   by ADR-0002.

All six have either a justification for remaining deferred or an ADR
resolution. No candidates with neither.

## 5. Glossary — FAIL

The ubiquitous-language glossary at `docs/02-classes.md:250-265`
defines: Prompt, Intent, Parameter, PrimaryFeature, Modifier, Adapter,
AdapterRefusal, Extras, Run, Run-id, Trace, Critique, SanityWarning,
ContractViolation.

One domain term used in a public class signature is undefined:

- `PromptsBundle` (`docs/02-classes.md:122`) — appears as a parameter
  type on `Planner.plan(client, prompt: str, model: str, prompts:
  PromptsBundle) PlanResult`, but is not defined anywhere in
  `docs/` (no glossary entry, no class diagram, no module-map row, no
  architecture mention). Either define it (likely a small dataclass
  wrapping `prompts/*.md` contents + hash) or rename the parameter to
  match an already-modeled type.

Other potentially-undefined terms checked and accepted:

- `DimensionMismatch`, `PlanResult`, `SanityResult`, `ExecutionResult`,
  `RunConfig`, `Tokens`, `ModelPrice` — each is defined as a class /
  dataclass in the same file (`02-classes.md:115-228`); not glossary
  candidates, they are typed structures rather than ubiquitous-language
  terms.
- Service names (`Planner`, `SanityChecker`, `Worker`, `Executor`,
  `Render`, `Loop`, `Pricing`, `Config`) — covered by the Services
  table at `02-classes.md:279-289`. Not duplicated in the glossary,
  acceptable.
- `intent_validation` — module name (covered in the module map and
  bounded-contexts table), not a domain term.

## 6. Phase readiness — PASS

`docs/phases/phase-1.md` is fully populated:

- Goal — `docs/phases/phase-1.md:19-21`.
- depends_on — frontmatter line 10 (`[phase-0]`); body line 22 reiterates.
- Minimum deliverable — `docs/phases/phase-1.md:61-88` (7 enumerated
  outputs).
- Maximum deliverable — `docs/phases/phase-1.md:90-107` (3 enumerated
  outputs).
- Sprint table — `docs/phases/phase-1.md:111-117` (5 rows: Day 1–5).
- Exit criteria — `docs/phases/phase-1.md:119-141` (10 enumerated
  criteria).

No empty slots.

## 7. Stale TODOs — PASS

Searched `docs/**/*.md` for `TBD`, `TODO`, `FIXME`, `<!-- gap:` while
excluding `docs/notes/` and `docs/audits/`:

```
grep -rEn "TBD|TODO|FIXME|<!-- gap:" docs/ --include="*.md" \
  | grep -v "^docs/notes/" | grep -v "^docs/audits/"
```

Returns no hits. Stale-marker count outside the excluded directories
is zero.

(Note: `docs/notes/inbox.md:1291` contains a `TODO:` in migrated vault
prose, but `notes/` is excluded by the audit rule. The TODO has an
explicit resolution marker recorded in `docs/notes/decisions.md`
2026-05-16 `/pm-architecture` G3 and resolved in ADR-0003, so it's
not stale.)

## 8. Notes hygiene — PASS

All seven `### ↪ From: …` migration sections in
`docs/notes/inbox.md` carry `> absorbed into …` markers and the
absorbed substance is present in the targets:

- `inbox.md:25-147` (Projects/Maquette/00-vision.md) — absorbed into
  `docs/00-vision.md` + `docs/00-pr-faq.md`. Vision sections (problem,
  dream, scope, non-goals, audience, success-criteria, strategic-fit)
  all present at `docs/00-vision.md:9-131`. Revisions per
  /pm-vision decisions (NX→v0.1, three-prompt success criterion,
  trimmed strategic-fit) match `00-vision.md:52-56,105-114,116-131`.
- `inbox.md:149-342` (01-architecture.md) — NFRs absorbed into
  `docs/01-requirements.md:59-69` (N1–N10); C4 / layered / repo
  layout / cross-cutting absorbed into
  `docs/02-architecture.md:15-234`.
- `inbox.md:344-611` (02-intent-schema.md) — absorbed into
  `docs/02-data-model.md` (ERD, entity defs, per-kind contracts,
  pydantic skeleton, evolution rules) + the Intent class diagram at
  `docs/02-classes.md:33-65`.
- `inbox.md:613-873` (03-agent-loop.md) — failure modes absorbed into
  `docs/01-requirements.md:208-218` (R1–R6); sequence diagrams +
  state machine absorbed into `docs/02-architecture.md` and the
  pipeline classes at `docs/02-classes.md:113-183`.
- `inbox.md:875-1131` (04-adapters.md) — absorbed into
  `docs/02-architecture.md` (Layered responsibilities + Dependency
  rules) and `docs/02-classes.md:67-109` (Adapter Protocol,
  Build123dTarget, AdapterRefusal) + dependency rules
  `02-classes.md:295-304`.
- `inbox.md:1133-1326` (05-cli-and-io.md) — absorbed into
  `docs/01-requirements.md` F1, F5, F8–F14, N2, N10; deferred items
  (inspect/list/replay, --no-nx/--only-nx, exit code 14) recorded
  under "Deferred from v0" at `01-requirements.md:32-54`.
- `inbox.md:1328-end` (06-roadmap.md) — absorbed into
  `docs/03-roadmap.md` (milestones, phases, open decisions, repo
  hygiene). Revisions (NX→v0.1, sanity check added, sandboxing
  deferred, Evaluator before NX in v0.1) match
  `03-roadmap.md:101-111`.

No un-absorbed sections were found that contradict the formal docs.

## 9. Frontmatter validity — PASS

- `docs/03-roadmap.md:1-4` — parses as YAML; keys `project_status`,
  `active_phase` present and valid (`building`, `null`).
- `docs/phases/phase-0.md:1-12` — parses; required keys `id`, `title`,
  `status`, `started`, `finished`, `min_goal_met`, `max_goal_met`,
  `blocker`, `depends_on`, `audit` all present. `status: done` consistent
  with `finished: 2026-05-17`.
- `docs/phases/phase-1.md:1-12` — parses; required keys present;
  `status: planned`, `depends_on: [phase-0]`, all date / outcome fields
  `null` as expected for a phase that hasn't started.
- `docs/phases/phase-0-report.md:1-8` — parses; report-style frontmatter
  (`phase`, `status`, `min_met`, `max_met`, `duration_planned_days`,
  `duration_actual_days`) consistent with the phase-0 plan.
- `docs/blockers/` — only `README.md` exists; no blocker docs to
  validate.

No parse errors and no missing required keys for the respective
templates.

Summary: 7 PASS, 2 FAIL

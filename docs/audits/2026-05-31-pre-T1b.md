---
audit: pre-phase
phase: T1b
date: 2026-05-31
result: 4 PASS, 5 FAIL
---

# Pre-phase audit — Touch (target phase T1b)

> Independent Auditor sub-agent, no conversation context. Verbatim report.

*Auditor read the design docs cold. F-IDs F1–F31 (no F-gap beyond intentional numbering), N-IDs N1–N12.*

## 1. Coverage — FAIL

Every F-ID maps to at least one named component **except**:

- **F2** (`docs/01-requirements.md:20` — VS-Code-like layout: viewport + tree + prompt + Settings, usable on 1920×1080): no component or class owns the overall window layout / app-shell composition. The four surfaces exist individually (`web/viewport`, `web/file-tree`, `web/prompt`, `web/settings` in `docs/02-classes.md:35-41`), and React+Vite is named as the layout tech (`docs/02-architecture.md:185`), but there is no `web/main` / app-shell module row that owns the panel composition and 1920×1080 layout responsibility. `web/main.ts` appears only in the repo-layout tree (`docs/02-architecture.md:247`), never in the module map or class list.

Notes (covered, but only via component *responsibility text*, not an F-ID tag — acceptable):
- F8 → `document.append` + `web/doc-store` (`docs/02-classes.md:95,45`).
- F17 (cancel) → `session` cancel token + Cancellation cross-cutting concern (`docs/02-architecture.md:163,320`).
- F21 (structured ops/errors) → `intent` + `intent_validation` (`ContractViolation`) + `AdapterRefusal` (`docs/02-classes.md:22-23`).
- F26 / F28 → Distribution/Lifecycle bounded context (`docs/02-classes.md:273`).
- F30 → `config` out_root (`docs/02-architecture.md:173`).

## 2. NFR satisfaction — PASS

All of N1–N12 have a row in the "Non-functional requirements — design satisfaction" table (`docs/02-architecture.md:279-292`).

## 3. Reference integrity — FAIL

Broken links in active design/planning docs:

- `docs/phases/phase-T1b.md:17` — links to `../adr/0008-picking-face-identity.md`; the actual file is `0008-picking-**and**-face-identity.md`. The correct form is used everywhere else (`docs/02-data-model.md:50,99`, `docs/adr/README.md:55`). This is in the **target phase doc's `depends_on`** — load-bearing.

Broken links in historical docs (notes/ + audits/ + shipped-phase reports — flagged for completeness, lower priority; most are `src/maquette/...` paths that no longer exist after the `maquette → touch_backend` rename, plus template placeholders):
- `docs/audits/2026-05-16-pre-phase-0.md:28` → `phases/phase-N.md` (template placeholder).
- `docs/audits/2026-05-18-pre-phase-2a-v2.md:158`, `docs/audits/2026-05-17-pre-phase-1-v2.md:63` — wrong relative depth (`./02-classes.md`, `../../src/maquette/intent.py`).
- `docs/phases/phase-0-report.md`, `phase-1-report.md`, `phase-2a-report.md`, `phase-2b-report.md`, `phase-3-report.md`, `phase-T1a.md:38` — multiple `../../src/maquette/...` links now dead post-rename.
- `docs/notes/inbox.md` (269, 921, 1133, 1271, 1414) and `docs/notes/inspiration.md:10` (`](url)` placeholder) — stale notes links.

## 4. ADR coverage — PASS

All six "Decisions deferred" items (`docs/02-architecture.md:332-349`) carry an explicit inline justification for remaining deferred ("promote to ADR if contested", "defer until the UX demands it", etc.). No item is silently unaddressed. ADR-0001…0009 all exist and are referenced.

## 5. Glossary — FAIL

Domain terms used in `docs/02-classes.md` (class shapes / DDD analysis) missing from the ubiquitous-language table (`docs/02-classes.md:286-307`):

- **`Parameter`** — a value object in the aggregates table (`docs/02-classes.md:316`), a field on `TouchDocument` (`docs/02-classes.md:91`), and a data-model entity (`docs/02-data-model.md:139`), but has no glossary entry.
- **`ClarifyingQuestion`** — a class in the engine diagram (`docs/02-classes.md:119`) and a `PlanResult` outcome; appears only inside the `Planner` definition, not as its own term (the concept "Conversation" is defined, but the emitted-object term is not).

Implementation/plumbing class names not in the glossary (PlanResult, ConversationState/ConversationTurn, CancelToken, Tokens, ExecutionResult, Response, Viewport, Picker, Transport, DocStore, SelectionStore, PromptPanel, KeychainBridge, AdapterRefusal, Build123dTarget) are **not** flagged as failures — the table is scoped to ubiquitous-language/domain terms, and the parent concepts (Adapter, Conversation, Selection, etc.) are defined. Noted only that the glossary is not a complete class index.

## 6. Phase readiness (phase-T1b) — PASS

`docs/phases/phase-T1b.md` has all required non-empty slots: goal (l.15), depends_on (l.10 frontmatter + l.17 prose), Min deliverable (l.19), Max deliverable (l.21), exit criterion (l.23), sprint table with 7 rows (Days 1–6 + Max, l.29-35), and a known-risks section (l.39-46). (The only defect is the broken ADR-0008 link in §3, not a readiness gap.)

## 7. Stale TODOs — FAIL

Markers outside notes/ and audits/:

- `docs/02-classes.md:331` — "`Executor` … Subprocess (or in-process — **TBD** by packaging spike)". A decision exists in `docs/notes/decisions.md:593-594` (P3: chosen = subprocess/worker, "Resolves the 02-classes.md:331 TBD"), but the design doc was **never updated** to reflect it — the TBD is stale relative to the recorded decision. The two downstream references (`docs/phases/phase-T1a-report.md:98`, `docs/phases/phase-T1b.md:43`) both still point at this unresolved-looking TBD. Fix: edit `02-classes.md:331` to record "subprocess/worker (decided — decisions.md P3)".

## 8. Notes hygiene — FAIL

- `docs/notes/constraints.md:14-48` is marked `> absorbed into docs/02-data-model.md + docs/adr/0004 @ 2026-05-28`. The substance (the adapter STEP-export contract, expressed in Maquette's `Intent{features, modifiers, extras}` / `export_step` / `body`-reserved-name vocabulary) is still carried by **ADR-0004**, but the **`02-data-model.md` half of the claim is no longer substantiated**: the Touch re-baseline (`docs/02-data-model.md`, dated 2026-05-29, *after* the absorption) replaced `Intent{features,modifiers,extras}` with `Operation`/`OperationKind`/`params` and contains no `features`/`export_step`/export-contract content. The absorption pointer now over-claims; it should point at ADR-0004 only (and `extras`/`Parameter` survive only as a relief-valve mention at `docs/02-data-model.md:48,142`). Not a hard contradiction (ADR-0004 still holds the rule), but the absorbed-into target is stale.

Other absorbed markers spot-checked (inbox.md → 00-vision/00-pr-faq/02-architecture/02-data-model/01-requirements) resolve in substance.

## 9. Frontmatter validity — PASS

All files under `docs/phases/`, `docs/blockers/`, and `docs/03-roadmap.md` parse as valid YAML. All phase plan files carry `id`, `title`, `status`, `depends_on`. Both blockers (`2026-05-28-*`) are `status: resolved` with a complete key set (`id, phase, severity, status, discovered, resolved, re_entry`). Roadmap has `project_status: building` and `active_phase: null` (consistent — T1a closed, T1b not yet started). No parse errors, no missing required keys, exactly zero phases at `in_progress`.

---

Summary: 4 PASS, 5 FAIL

(FAIL: 1 Coverage, 3 Reference integrity, 5 Glossary, 7 Stale TODOs, 8 Notes hygiene. PASS: 2 NFR, 4 ADR, 6 Phase readiness, 9 Frontmatter.)

The two items that actually block confident T1b start are: the **broken ADR-0008 link in `phase-T1b.md:17`** (check 3) and the **stale executor-TBD in `02-classes.md:331`** (check 7) — the latter is squarely on T1b's critical path (R-T1b-5 wires `session → executor`). The rest (F2 layout owner, Parameter glossary entry, constraints.md absorption pointer) are doc-quality fixes that don't gate implementation.

## Override

- **When:** 2026-05-31
- **By:** vandpaolo (delegated judgment to Claude)
- **Fixed before start (the two blocking FAILs):**
  - #3 — corrected the broken ADR-0008 link in `phase-T1b.md:17` (`0008-picking-and-face-identity.md`).
  - #7 — resolved the executor TBD in `02-classes.md:331` to "subprocess/worker (decided — decisions.md 2026-05-31 P3)".
- **Overridden (doc-quality, none gates T1b's backend scope F19/F20/F21/F22/F31):**
  - #1 Coverage — F2 is the VS-Code *frontend* layout (T2 territory); no `web/main` app-shell owner yet. Carryover: add it via `/pm-architecture` before T2. T1b ships no frontend.
  - #5 Glossary — `Parameter` / `ClarifyingQuestion` missing from the ubiquitous-language table; vocabulary hygiene, doesn't gate implementation. Fold into a glossary pass.
  - #8 Notes hygiene — `constraints.md` "absorbed into 02-data-model.md" pointer over-claims post-pivot (same long-standing item as pre-T1a #8). Annotate to point at ADR-0004 only; non-blocking.
  - #3 (historical) — `../../src/maquette/...` links in *closed* phase reports + notes are dead post-rename. Those docs are locked historical records (never edited per framework); expected, not fixed.
- **Decision:** Override and start T1b.

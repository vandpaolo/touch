# Pre-phase audit — phase-1 (v3, independent cold review)

Date: 2026-05-17
Auditor: cold-read (no conversation context; prior audit files not read)
Target phase: `docs/phases/phase-1.md`

## 1. Coverage — PASS

All 14 functional requirements (F1–F14) map to at least one component or
class in `docs/02-architecture.md` and/or `docs/02-classes.md`:

- F1 (CLI accepts prompt) → `maquette.cli` (`02-classes.md:18`,
  `02-architecture.md:123`).
- F2 (Planner → validated Intent) → `agent.planner.plan(...)` returning
  `PlanResult` (`02-classes.md:20`, `02-architecture.md:114`).
- F3 (Worker emits build123d via adapter) → `agent.worker.emit_code` +
  `adapters.build123d_target.emit` (`02-classes.md:22, 25`).
- F4 (full v0 Intent schema) → per-kind helpers `_emit_box…_emit_pattern`
  on `Build123dTarget` (`02-classes.md:84-103`).
- F5 (Executor → STEP) → `Executor.execute` returning `ExecutionResult`
  (`02-classes.md:154-158`).
- F6 (dimension sanity check) → `agent.sanity.SanityChecker.check`
  (`02-classes.md:137-141`).
- F7 (3 orthographic PNGs) → `render.orthographic` (`02-classes.md:27,
  189-194`).
- F8 (run folder artefact set) → `Loop` writers (`02-classes.md:167-175`).
- F9 (status.json + cost) → `Loop._write_status` + `Pricing.price`
  (`02-classes.md:171, 209`).
- F10 (trace.jsonl per state) → `Loop._write_trace` (`02-classes.md:170`).
- F11 (run-id format) → `Loop._new_run_dir` (`02-classes.md:169`).
- F12 (CLI prints run dir on exit) → `cli` (`02-architecture.md:123`).
- F13 (exit code table) → `cli` exit-code mapping (`02-architecture.md:123`,
  test row `02-classes.md:321`).
- F14 (CLI flags) → `cli` + `Config` (`02-classes.md:18, 219-227`).

No orphan F-IDs.

## 2. NFR satisfaction — PASS

All 10 NFRs (N1–N10) have a row in the "Non-functional requirements —
design satisfaction" table at `02-architecture.md:199-210`:

- N1 → `02-architecture.md:201`
- N2 → `02-architecture.md:202`
- N3 → `02-architecture.md:203`
- N4 → `02-architecture.md:204`
- N5 → `02-architecture.md:205`
- N6 → `02-architecture.md:206`
- N7 → `02-architecture.md:207`
- N8 → `02-architecture.md:208`
- N9 → `02-architecture.md:209`
- N10 → `02-architecture.md:210`

No orphan N-IDs.

## 3. Reference integrity — PASS

All cross-doc markdown links in `docs/**/*.md` resolve:

- `02-architecture.md:76` → `./02-classes.md#dependency-rules` resolves
  (anchor at `02-classes.md:292`).
- `02-architecture.md:258, 264, 269-271` → ADR files exist
  (`docs/adr/0001-…`, `0002-…`, `0003-…`).
- `02-data-model.md:25` → `./adr/0001-intent-as-pivot.md` exists.
- `03-roadmap.md:60` → `phases/phase-0.md` exists.
- `adr/0001-intent-as-pivot.md:46-47` → `../02-data-model.md`,
  `../02-architecture.md`, `../02-classes.md` all exist.
- `adr/README.md:43-45` → all three ADR files exist.
- `phases/phase-0-report.md:13` → `phase-0.md` exists; `:14` →
  `../audits/2026-05-16-pre-phase-0.md` exists; `:20-24` → root files
  `pyproject.toml`, `LICENSE`, `README.md`, `.gitignore`, `.env.example`,
  `examples/README.md`, `.github/workflows/ci.yml`, `pyrightconfig.json`,
  `.pre-commit-config.yaml`, and `src/maquette/{intent,intent_validation,
  pricing,config}.py` + matching `tests/test_*.py` all exist.
- `phases/phase-1.md:22, 30, 44, 100, 174` → `phase-0.md`,
  `../02-classes.md`, `phase-0-report.md` all exist.

Notes-folder links (`docs/notes/inbox.md` references to vault paths like
`./02-intent-schema.md`, `./03-agent-loop.md`) are intra-note references
to migrated-vault prose and are not cross-doc design links — scope of the
check is `docs/**/*.md` excluding notes/audits per the framework's
"docs-out" treatment, and the migrated vault inserts are clearly framed
as historical excerpts. No broken cross-doc links.

## 4. ADR coverage — PASS

`02-architecture.md` § Decisions deferred (lines 238-266) contains 6
entries. Status check:

1. Provider abstraction — flagged as not getting an ADR yet, with clear
   "Tracked in vision § Non-goals; no separate ADR yet" justification
   (`02-architecture.md:246`). Justified deferral.
2. Conversational refinement vs one-shot — explicitly deferred to v0.2
   planning with rationale (`02-architecture.md:247-249`). Justified.
3. NX adapter feature coverage — explicit non-goal scope statement
   (`02-architecture.md:250-253`). Justified.
4. Sandboxing strategy — explicit "will be an ADR if it does" condition
   (`02-architecture.md:254-256`). Justified deferral.
5. Prompt versioning approach — **Resolved** by ADR 0003
   (`02-architecture.md:257-262`). Linked.
6. SanityCheck tolerance — **Resolved** by ADR 0002
   (`02-architecture.md:263-266`). Linked.

No candidates lack either an ADR or a justification.

## 5. Glossary — FAIL

Glossary at `02-classes.md:250-266` defines: Prompt, Intent, Parameter,
PrimaryFeature, Modifier, Adapter, AdapterRefusal, Extras, Run, Run-id,
Trace, Critique, SanityWarning, ContractViolation, PromptsBundle.

Terms used in class names / method names / DDD analysis in
`02-classes.md` that do **not** appear in the glossary:

- **Planner** — class name (`02-classes.md:120-125`) and service row
  (`02-classes.md:282`). Heavily load-bearing domain role; glossary
  defines "Prompt" and "Intent" but not the agent that translates one
  to the other.
- **SanityChecker** — class (`02-classes.md:137-141`); service
  (`02-classes.md:283`). The glossary defines `SanityWarning` (its
  output) but not the actor.
- **Worker** — class (`02-classes.md:142-146`); service
  (`02-classes.md:285`). Domain role used in ADR 0001 and architecture
  prose.
- **Executor** — class (`02-classes.md:154-158`); service
  (`02-classes.md:286`). Distinct from `ExecutionResult`.
- **Loop** — class (`02-classes.md:167-175`); service
  (`02-classes.md:288`). Central orchestrator role.
- **Render / Renderer** — module/class (`02-classes.md:189-194`);
  service (`02-classes.md:287`). F7/N5 vocabulary.
- **Evaluator** — referenced throughout (`02-classes.md:24` and
  architecture); glossary defines `Critique` (its output) but not the
  role itself, even though it's v0.1 scope.
- **PlanResult** — return value (`02-classes.md:115-119`); not defined.
- **SanityResult** — return value (`02-classes.md:126-130`); not
  defined. Distinct from `SanityWarning`.
- **DimensionMismatch** — value object (`02-classes.md:131-136`); not
  defined.
- **ExecutionResult** — return value (`02-classes.md:147-153`); not
  defined.
- **RunConfig** — config struct (`02-classes.md:159-166`); not defined,
  and is *not* the same as `Config` (the infrastructure dataclass at
  `02-classes.md:219-227`); the distinction matters.
- **Tokens** — dataclass (`02-classes.md:201-206`); used in `status.json`
  vocabulary throughout `01-requirements.md` (F9, F10). Not defined.
- **ModelPrice** — dataclass (`02-classes.md:212-217`); not defined.

Some of these (PlanResult, SanityResult, ExecutionResult, Tokens,
ModelPrice, RunConfig, DimensionMismatch) are arguably internal
type-record names rather than ubiquitous-language terms — but Planner,
SanityChecker, Worker, Executor, Loop, Render, and Evaluator are
clearly ubiquitous-language roles and ought to be glossed.

## 6. Phase readiness — PASS

`docs/phases/phase-1.md` slots:

- Goal — non-empty (`phase-1.md:19-21`).
- depends_on — non-empty: `[phase-0]` in frontmatter (`phase-1.md:10`)
  and prose dependency list (`phase-1.md:22-25`).
- Minimum deliverable — non-empty, 7 bullets (`phase-1.md:61-88`).
- Maximum deliverable — non-empty, 4 bullets (`phase-1.md:90-107`).
- Sprint table — 5 rows (Days 1–5), all with Task / Output / Done-when
  filled (`phase-1.md:113-117`).
- Exit criteria — 10 items (`phase-1.md:119-141`).

All required slots are non-empty.

## 7. Stale TODOs — PASS

`grep -rn -E "TBD|TODO|FIXME|<!-- gap:"` across `docs/**/*.md` excluding
`notes/` and `audits/` returns no matches. No stale markers.

## 8. Notes hygiene — FAIL

Only one absorbed-marker exists: `notes/inbox.md:27` —
`> absorbed into docs/00-vision.md and docs/00-pr-faq.md @ 2026-05-16`,
attached to the `### ↪ From: Projects/Maquette/00-vision.md` subsection.

The other five migrated-vault subsections do **not** carry their own
absorbed markers, despite having been the source material for the
formal docs:

- `notes/inbox.md:149` — `### ↪ From: Projects/Maquette/01-architecture.md`:
  content absorbed into `02-architecture.md` (C4 levels, Layered
  responsibilities, Tech stack, Repo layout, NFRs, Cross-cutting). No
  marker. Also contains an outdated NFR mention of NX in v0 latency
  budgeting (vault § Non-functional requirements) that is now
  contradicted by `01-requirements.md` N1 (`< 20 s p95 for v0` because
  NX moved to v0.1) — no marker explains the divergence.
- `notes/inbox.md:344` — `### ↪ From: Projects/Maquette/02-intent-schema.md`:
  content absorbed into `02-data-model.md` (entity definitions, per-kind
  contracts, pydantic skeleton, schema-evolution policy). No marker.
- `notes/inbox.md:613` — `### ↪ From: Projects/Maquette/03-agent-loop.md`:
  content absorbed into `02-architecture.md` § C4-Level-3 +
  `01-requirements.md` (state machine names, failure-mode taxonomy →
  exit-code table F13). No marker.
- `notes/inbox.md:875` — `### ↪ From: Projects/Maquette/04-adapters.md`:
  content absorbed into `02-architecture.md` § Layered responsibilities
  + `02-classes.md` § Adapter Protocol diagram + ADR 0001. No marker.
- `notes/inbox.md:1133` — `### ↪ From: Projects/Maquette/05-cli-and-io.md`:
  content absorbed into `01-requirements.md` F1, F8-F14 +
  `02-architecture.md` § Cross-cutting concerns. No marker.
- `notes/inbox.md:1328` — `### ↪ From: Projects/Maquette/06-roadmap.md`:
  content absorbed into `03-roadmap.md` (milestone overview, phase
  outlines). No marker.

Substantive contradictions between unmarked vault sections and current
formal docs:

- `inbox.md:1373` "v0 — Single-shot prompt to part" (vault roadmap)
  includes the NX adapter and `--no-nx` / `--only-nx` flags in v0.
  Current `03-roadmap.md` and `00-vision.md` put NX in v0.1 (per decision
  B1, `notes/decisions.md:73-76`). Vault subsection has no absorbed
  marker noting this revision.
- `inbox.md:302` vault § Non-functional requirements quotes a 30 s
  latency target for v0; current `01-requirements.md:60` is 20 s p95 for
  v0. Unmarked.
- `inbox.md:1147` vault § CLI surface (v0) lists `maquette inspect`,
  `maquette list`, `maquette replay` as v0 commands; current
  `01-requirements.md:39-43` defers all three to v0.1. Unmarked.

The parent `## ↪ Migrated from vault @ 2026-05-16` block (line 17) has a
collective preamble framing this as seed material, but per the
framework rule each section that has been folded forward should carry
its own `> absorbed into …` marker so the audit trail is per-section,
not just per-block.

## 9. Frontmatter validity — PASS

- `docs/03-roadmap.md:1-4` — `---` block parses; keys: `project_status:
  building`, `active_phase: null`. Both keys present and well-formed.
  Inline `#` comments are valid YAML.
- `docs/phases/phase-0.md:1-12` — parses; all expected keys present:
  `id, title, status, started, finished, min_goal_met, max_goal_met,
  blocker, depends_on, audit`. Status `done`, dates `2026-05-16` /
  `2026-05-17`, `audit: audits/2026-05-16-pre-phase-0.md`.
- `docs/phases/phase-0-report.md:1-8` — parses; keys: `phase, status,
  min_met, max_met, duration_planned_days, duration_actual_days`. Valid.
- `docs/phases/phase-1.md:1-12` — parses; all expected keys present:
  `id, title, status, started, finished, min_goal_met, max_goal_met,
  blocker, depends_on, audit`. Status `planned`, `started: null`,
  `finished: null`, `depends_on: [phase-0]`.
- `docs/blockers/` — only `README.md` present (no frontmatter, but it
  is a directory README rather than a blocker doc); no blocker files
  exist yet, so nothing to fail on.

All frontmatter under `docs/phases/` and `docs/03-roadmap.md` parses
cleanly with required keys present per the framework templates.

---

Summary: 7 PASS, 2 FAIL

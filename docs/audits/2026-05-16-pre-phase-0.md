# Pre-phase-start Audit — phase-0

- **Date:** 2026-05-16
- **Phase:** phase-0 (Foundations)
- **Auditor:** general-purpose sub-agent (independent context)
- **Initial result:** FAIL — 4 issues across 3 checks
- **Final result:** PASS — all issues resolved
- **Status flip authorised:** yes — phase-0.md frontmatter `status: planned`
  → `status: in_progress`, `started: 2026-05-16`

## Checks (per Framework § 9)

### 1. Coverage (F1–F14 → components / classes)
**PASS.** All 14 functional requirements map to at least one component
in `02-architecture.md` or class in `02-classes.md`.

### 2. NFR satisfaction (N1–N10)
**PASS.** All 10 NFRs have entries in
`02-architecture.md § Non-functional requirements — design satisfaction`.

### 3. Reference integrity
**INITIAL FAIL → RESOLVED.**

| # | Issue | Resolution |
|---|---|---|
| 1 | `docs/adr/0001-intent-as-pivot.md:46` linked to `../02-intent-schema.md` (vault path, file does not exist in framework layout) | Updated to `../02-data-model.md` |
| 2 | `docs/adr/0001-intent-as-pivot.md:47` linked to `../04-adapters.md` (vault path) | Updated to `../02-architecture.md` + `../02-classes.md` |
| 3 | `docs/03-roadmap.md` had markdown links to nonexistent `phases/phase-1.md`, `phase-2a.md`, `phase-2b.md`, `phase-3.md`, `phase-3.5.md` (marked "not yet drafted" but technically broken) | Converted from `[phase-N.md](phases/phase-N.md)` to plain-text `` `phases/phase-N.md` ``. Forward references stay visible; no broken links. |

### 4. ADR coverage
**PASS.** The Adapter Protocol (in-text + decision P2), the
intent/intent_validation split (in-text + decision B3), filesystem-as-state
(in-text architecture § Cross-cutting / State), prompt caching (ADR 0003),
sanity check (ADR 0002), Intent as the pivot (ADR 0001) all have either
an ADR or a clear in-text justification.

### 5. Glossary / ubiquitous language
**INITIAL FAIL → RESOLVED.**

| # | Issue | Resolution |
|---|---|---|
| 1 | `ContractViolation` referenced in `02-classes.md` module map + dependency rules but absent from § Ubiquitous language | Added to the glossary table |

### 6. Phase-0 readiness
**PASS.** Frontmatter is valid YAML with all required fields. Goal,
depends_on, MIN, MAX, sprint/day breakdown, exit criteria, known risks
all present.

### 7. Stale TODOs
**INITIAL FAIL → RESOLVED.**

| # | Issue | Resolution |
|---|---|---|
| 1 | `docs/notes/inbox.md:1291` — `TODO: write prompt_versions into status` in migrated vault content. Resolution exists (decisions.md /pm-architecture G3 → ADR-0003) but no resolution marker on the source line | Added inline HTML comment marker: `<!-- RESOLVED 2026-05-16: single SHA-256 of prompts/ contents → status.json.prompts_hash --> ` |

### 8. Notes hygiene
**PASS.** Spot-checks of three "absorbed" markers (00-vision → 00-vision.md,
01-architecture NFRs → 01-requirements.md, 02-intent-schema → 02-data-model.md)
all confirm the content claimed absorbed is present in the named target.

### 9. Frontmatter validity
**PASS.** `phases/phase-0.md` and `03-roadmap.md` both parse as valid
YAML with all framework-required fields.

## Override record

None. No checks were overridden by the user. All FAILs were resolved
before status flip.

## Summary

Pre-phase-0 audit ran, surfaced 4 mechanical issues (broken links, a
missing glossary term, an unresolved migration TODO marker), all fixed
with one-line edits. Final state: PASS on all 9 checks. Phase-0 is
greenlit; status flips to `in_progress` on 2026-05-16.

The fact that all 4 issues were mechanical (and the only category that
flagged was reference/glossary hygiene, not design coverage or NFR
satisfaction) is a good signal — the design rounds did their job.

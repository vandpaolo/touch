# Pre-phase-3.5 audit — 2026-05-28

> Independent Auditor sub-agent review (no conversation context, docs read
> cold) run via `/pm-audit phase-3.5` ahead of `/pm-phase-start`.

## 1. Coverage — PASS

Every functional requirement F1–F14 maps to at least one component in `docs/02-architecture.md` / `docs/02-classes.md`:

- F1, F12, F13, F14 (CLI, run-dir print, exit codes, flags) → `maquette.cli` (`02-classes.md:18`)
- F2 (prompt → Intent) → `maquette.agent.planner` (`02-classes.md:20`)
- F3, F4 (build123d emission, full 11-kind schema) → `maquette.adapters.build123d_target` + `maquette.agent.worker` (`02-classes.md:22,25`)
- F5 (Executor → STEP) → `maquette.agent.executor` (`02-classes.md:23`)
- F6 (dimension sanity) → `maquette.agent.sanity` (`02-classes.md:21`)
- F7 (3 renders) → `maquette.render` (`02-classes.md:27`)
- F8, F9, F10, F11 (run folder, status.json, trace.jsonl, run-id) → `maquette.agent.loop` (`02-classes.md:19`, `02-classes.md:166-174`)

No orphan F-IDs.

## 2. NFR satisfaction — PASS

All ten NFRs (N1–N10) from `docs/01-requirements.md:60-69` have a row in the "Non-functional requirements — design satisfaction" table at `docs/02-architecture.md:199-210` (N1 line 201 through N10 line 210). No orphan N-IDs.

## 3. Reference integrity — PASS

All cross-doc relative markdown links in the in-scope docs resolve:
- `02-architecture.md` → `./adr/0001`–`0003`, `./02-classes.md#dependency-rules` (anchor present at `02-classes.md:318` `## Dependency rules`)
- `02-classes.md:266` → `./adr/0004-build123d-export-variable.md` (exists)
- `02-data-model.md:25,238` → `./adr/0001`, `adr/0004` (exist)
- `03-roadmap.md:60` → `phases/phase-0.md` (exists)
- `phases/phase-3.5.md:27,158` → `phase-3.md`, `../03-roadmap.md` (exist)
- `adr/0001-intent-as-pivot.md:46-47` → `../02-data-model.md`, `../02-architecture.md`, `../02-classes.md` (exist)
- `adr/README.md:43-46` → `0001`–`0004` (all exist)

No broken links.

## 4. ADR coverage — PASS

The six "Decisions deferred" entries in `docs/02-architecture.md:237-266` each have either an ADR or an explicit justification for remaining deferred:
1. Provider abstraction — justified deferred (no second provider in scope; tracked in vision § Non-goals)
2. Conversational refinement — justified deferred (v0.2 scope)
3. NX adapter coverage — justified deferred (v0.1 scope, schema is the bottleneck)
4. Sandboxing strategy — justified deferred (v0.1+ ADR "if it does")
5. Prompt versioning — **Resolved** in ADR-0003
6. SanityCheck tolerance — **Resolved** in ADR-0002

No candidate lacks both.

## 5. Glossary — PASS

Every domain term used in `docs/02-classes.md` class names, method names, and DDD analysis appears in the ubiquitous-language glossary (`02-classes.md:257-292`): Intent, Parameter, PrimaryFeature, Modifier, Adapter, AdapterRefusal, Build123dTarget, NxOpenTarget, Extras, Run, Run-id, Trace, Critique, SanityWarning, ContractViolation, PromptsBundle, Planner, SanityChecker, Worker, Executor, Loop, Renderer, Evaluator, PlanResult, SanityResult, Dimension, DimensionMismatch, ExecutionResult, RunConfig, Tokens, ModelPrice, Pricing, Config. No undefined terms.

## 6. Phase readiness — PASS

`docs/phases/phase-3.5.md` has all required slots non-empty:
- goal — line 19
- depends_on — frontmatter line 10 (`[phase-3]`) + prose line 27
- minimum deliverable — lines 58-79
- maximum deliverable — lines 81-98
- sprint table — lines 102-105 (2 rows)
- exit criteria — lines 107-126 (7 numbered items)

## 7. Stale TODOs — PASS

`grep -rnE "TBD|TODO|FIXME|<!-- gap:"` across `docs/**/*.md` excluding `notes/` and `audits/` returns no matches. The only hits are inside `notes/` (decisions.md, inbox.md) and `audits/`, both excluded by the audit rule. No stale markers to triage.

## 8. Notes hygiene — PASS

The one note section marked absorbed in the live window — `notes/constraints.md:16` (`> absorbed into docs/02-data-model.md + docs/adr/0004 @ 2026-05-28`, the adapter export contract) — is substantively present in both targets: `docs/02-data-model.md:234-266` (§ Adapter export contract) and `docs/adr/0004-build123d-export-variable.md`. The note's own "first-draft / rejected uniform-body" caveat matches ADR-0004's "Alternatives considered." The other absorbed markers all live in `inbox.md` and trace to existing doc sections; no contradictions found between un-absorbed note content and the target docs.

## 9. Frontmatter validity — PASS

All files under `docs/phases/` (phase-0, 1, 2a, 2b, 3, 3.5 + their reports), `docs/03-roadmap.md`, and `docs/blockers/` (only README, no blocker docs) have a well-formed leading `---`…`---` block. Required phase keys present on every phase file: `id`, `title`, `status`, `started`, `finished`, `min_goal_met`, `max_goal_met`, `blocker`, `depends_on`, `audit`. `03-roadmap.md` has `project_status` + `active_phase`. No parse errors, no missing keys.

---

Summary: 9 PASS, 0 FAIL

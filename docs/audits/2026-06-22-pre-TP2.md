# Pre-phase audit — TP2 (MCP server + agent loop, MCP-first)

- **Date:** 2026-06-22
- **Target phase:** TP2 (document cutover → MCP server → agent loop)
- **Auditor:** general-purpose sub-agent (cold read)
- **Context:** TP1 closed `done` 2026-06-22 (min met, max partial); the live
  document cutover + F45 packets were re-scoped into TP2 sprint 1 per
  blocker `2026-06-22-tp1-bridge-rescope`.

---

## 1. Coverage — PASS

Every requirement TP2 delivers (F41, F42, F43, F44, F45, N14, N15, N16 — plus
F46, carried/hardened from TP1) maps to at least one named component:

- F41 (MCP tools + structured mutating envelope) → `mcp_server`
  (`02-architecture.md:223`, `02-classes.md:37`).
- F42 (own Claude Code over MCP, zero API tokens) → `mcp_server`
  (`02-architecture.md:223`); N14 row `02-architecture.md:362`.
- F43 (one brain, two surfaces; ephemeral positional subagent) →
  `web/agent-panel` (`02-architecture.md:227`) + ADR-0015.
- F44 (one shared live doc, versioned, CAS) → `layer_stack`
  (`02-architecture.md:220`, `02-classes.md:32`).
- F45 (finder-ref + positional/macro context packets) → `context_packets`
  (`02-architecture.md:224`, `02-classes.md:38`).
- F46 (workspace-confined executor, single chokepoint) → `agent.executor`
  (`02-architecture.md:225`, `02-classes.md:26`).
- N14/N15/N16 satisfied by `mcp_server` / `context_packets` / `layer_stack`
  (see Check 2).

The two not-yet-built TP2 modules are explicitly flagged in the module map:
`mcp_server` and `context_packets` both carry **"NOT BUILT — TP2"**
(`02-classes.md:37,38`) with planned signatures listed. No coverage gap.

## 2. NFR satisfaction — PASS

TP2's three NFRs each have a dedicated row in the design-satisfaction table:
N14 (`02-architecture.md:362`), N15 (`02-architecture.md:363`), N16
(`02-architecture.md:364`). Each names the mechanism (MCP subscription auth /
id-referenced compact manifest + thumbnails-on-demand + byte-stable prefix /
versioned CAS, single writer). No orphan NFRs.

## 3. Reference integrity — FAIL (accepted-historical)

The full `docs/**/*.md` link sweep finds broken relative links, **all in frozen
notes/audits, none in the active design docs** (`00/01/02/03/adr` are clean):

- `docs/notes/inbox.md` → `./02-intent-schema.md` (×3), `./03-agent-loop.md`
  — Maquette-era filenames, never created post-pivot.
- `docs/notes/inspiration.md` → `url` ; `docs/audits/2026-05-31-pre-T1b.md`
  → `url` — literal ellipsis/placeholder tokens, not real links.
- `docs/audits/2026-05-16-pre-phase-0.md` → `phases/phase-N.md` — template
  placeholder.
- `docs/audits/2026-05-31-pre-T2.md` → `../../src/maquette/…`,
  `../../src/touch_backend/…` — literal `…` ellipsis, not paths.
- `docs/audits/2026-05-17-pre-phase-1-v2.md` → `./02-classes.md` (wrong depth);
  `docs/audits/2026-05-18-pre-phase-2a-v2.md` → `../../src/maquette/intent.py`
  (pre-pivot path).

Same disposition as the TP1 audit (Check 3): these live in frozen
Maquette-era / historical-audit files preserved by project policy. **No broken
link in any active design doc.** Mechanically a FAIL; effectively
accepted-historical.

## 4. ADR coverage — PASS

All four ADRs TP2 depends on exist and are **Accepted**: ADR-0013 (shared live
doc + versioned stack, marked *deferred-live* — exactly what TP2 sprint 1 wires),
ADR-0014 (MCP boundary), ADR-0015 (conversation topology / context packets),
ADR-0016 (executor sandboxing). The "Decisions deferred" block
(`02-architecture.md:403–428`) holds 6 entries, each with a justification for
staying deferred (FE framework, binary mesh frame, WS auth, op-history
granularity, multi-doc sessions, dev sidecar process model). **None of the 6 is
forced by TP2** — TP2's design decisions are already promoted to ADRs 0013–0016.

## 5. Glossary — PASS

Every domain term TP2 introduces/relies on is defined in the ubiquitous-language
glossary in `02-classes.md`: **Layer Stack** (`:348`), **MCP server** (`:351`),
**Context packet** (`:352`), **1-ring** (`:353`), **Stack revision** (`:354`),
plus **Mesh cache / `mesh_cache`** (`:338`, the term the TP1 audit added). The
two Check-5 gaps from the TP1 audit (`mesh_cache`, `1-ring`) are both resolved.
No undefined TP2 term.

## 6. Phase readiness — PASS

`docs/phases/phase-TP2.md` has every required slot non-empty: goal (`:15`),
frontmatter `depends_on: [TP1]` (`:10`) + a "Depends on" section (`:44–57`),
minimum deliverable (`:59–81`), maximum deliverable (`:83–88`), a 3-sprint /
10-day sprint table (`:92–115`), exit criteria (`:117–128`), and known risks
(`:130–168`). Frontmatter is well-formed. The dependency on TP1 is satisfied —
the TP1 report (`phase-TP1-report.md:51`) explicitly hands the live cutover +
F45 packets to "TP2 sprint 1", matching this plan's sprint-1 framing.

## 7. Stale TODOs — PASS

No `TBD`/`TODO`/`FIXME`/`<!-- gap:` markers in the active design docs
(`00/01/02/03/adr`). The `NOT BUILT — TP2` annotations on `mcp_server` /
`context_packets` (`02-classes.md:37,38`) are intentional forward-status labels,
not stale markers.

## 8. Notes hygiene — PASS

All `> absorbed into …` markers in `docs/notes/` point at targets whose
substance is present (constraints→data-model+ADR-0004; questions→requirements
F18/F32/F33/F34; inbox→vision/pr-faq/requirements/architecture/data-model/ADRs
0005,0006,0009). No sampled marker contradicts its target. TP2-relevant
requirements (F41–F46, N14–N16) are all present in `01-requirements.md`
(`:83–88,108–110`).

## 9. Frontmatter validity — PASS

All files under `docs/phases/`, `docs/blockers/`, and `docs/03-roadmap.md` have
valid, closing YAML frontmatter with consistent key sets. `phase-TP2.md`
frontmatter is well-formed (`status: planned`, `depends_on: [TP1]`). The roadmap
has `active_phase: null` — consistent with a *pre*-phase audit (TP1 closed, TP2
not yet started). The rescope blocker `2026-06-22-tp1-bridge-rescope.md` is
`status: resolved`. Audit files carry no frontmatter (expected by template).

---

**Summary: 8 PASS, 1 FAIL**

The single FAIL is Check 3 (reference integrity) — every broken link is in a
frozen Maquette-era notes/audit file or is a literal ellipsis/placeholder token,
identical disposition to the TP1 audit. No active design doc has a broken link.

Effective status for greenlight: **8 PASS, 1 accepted-historical FAIL.** No
blocking finding. The design is TP2-ready: the cutover + MCP modules are named,
ADR-covered, glossary-defined, and the TP1 report cleanly hands the
sprint-1 cutover work forward.

## Override

- **Date:** 2026-06-22
- **By:** vandpaolo (via /pm-phase-start TP2, "go next")
- **Reason:** The lone FAIL (Check 3, reference integrity) is confined to frozen
  Maquette-era / placeholder links in `docs/notes/` and pre-pivot `docs/audits/`
  files — no active design doc (`00/01/02/03/adr`) is affected. This is the same
  accepted-historical disposition under which TP1 was greenlit. Non-blocking;
  proceeding to flip TP2 to in_progress.

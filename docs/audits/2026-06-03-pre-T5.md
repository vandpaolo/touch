---
audit: pre-T5
date: 2026-06-03
phase: T5
result: 8 PASS, 1 FAIL
---

# Pre-T5 audit — independent review

> Auditor sub-agent, no conversation context. Verbatim report.

## 1. Coverage — PASS

All F1–F37 are implemented by at least one named component (`docs/02-architecture.md`) or module/class (`docs/02-classes.md`). Six F-IDs (F8, F17, F21, F26, F28, F30) lack a literal `Fxx` tag but are each clearly owned by a component:
- F8 → `document.append` + `doc-store`/`viewport` mesh update
- F17 → `session` cancel token + `prompt` cancel (`02-architecture.md:352`)
- F21 → `intent_validation.ContractViolation` + `adapters.AdapterRefusal`
- F26 → Distribution context: GH Actions, semver tags, `release.yml`
- F28 → `electron-builder` + PyInstaller installer (`02-architecture.md:212`)
- F30 → `config.out_root` default `/srv/touch/` (`02-architecture.md:193`)

No orphan F-IDs.

## 2. NFR satisfaction — PASS

N1–N13 each have a row in the "Non-functional requirements — design satisfaction" table (`docs/02-architecture.md:305-321`). No orphan N-IDs.

## 3. Reference integrity — PASS

All cross-doc relative links and anchors in the live design docs (`00-*`, `01-*`, `02-*`, `03-roadmap.md`, `phases/`, `blockers/`, `adr/`) resolve. The only unresolved targets are in `docs/notes/` and `docs/audits/` (out of scope) and are illustrative `…`/historical-path strings, not live design links.

## 4. ADR coverage — PASS

All six "Decisions deferred" (`docs/02-architecture.md:360-381`) carry an explicit justification for remaining deferred (FE framework, binary mesh frame, WS auth, history granularity, multi-doc sessions, dev sidecar process model — each with a "promote to ADR when X" / "defer until Y" rationale). None is silently dropped.

## 5. Glossary — FAIL

Domain terms used in class names, method signatures, and the DDD analysis of `docs/02-classes.md` that are absent from the ubiquitous-language glossary (`docs/02-classes.md:304-331`):
- `FinderPredicate` — value object in the aggregates table (`02-classes.md:340`), `Selection.finder` type (`:114`), `intent` public surface (`:21`). Distinct from the defined "Finder".
- `ConversationState` — transient entity (`02-classes.md:343`), `Session.conversation` type (`:81`). Distinct from the defined "Conversation".
- `ConversationTurn` — value used in `Operation.conversation` (`:108`) and `PromptPanel.thread` (`:234`).
- `PlanResult` — planner return type (`02-classes.md:19`, `:118`).
- `TopoEntity` — `Selection.resolve()` return type (`02-classes.md:116`).
- `FinderError` — `finder` public surface / resolution outcome (`02-classes.md:23`, `:131`).

## 6. Phase readiness — PASS

`docs/phases/phase-T5.md` has non-empty goal (`:18`), depends_on (frontmatter `[T4]` + `:23-30`), minimum deliverable (`:31-41`), maximum deliverable (`:43-46`), sprint table with 8 rows (`:50-59`), and exit criterion (`:21`).

## 7. Stale TODOs — PASS

No live `TBD`/`TODO`/`FIXME`/`<!-- gap:` markers in the current design docs (`00`/`01`/`02`/`03`). The three TBD occurrences (`phase-T1a-report.md:98`, `phase-T1b-report.md:52,96`, `phase-T1b.md:43`) are historical references to the executor process-model TBD, which was resolved and recorded in `decisions.md` (2026-05-31 P3) and now appears as a settled decision at `02-classes.md:355`.

## 8. Notes hygiene — PASS

All `> absorbed into …` sections checked land in their target docs in substance (e.g. `questions.md:97` → F18/F32/F33/F34 present in `01-requirements.md`; `constraints.md:16` → adapter export contract present in ADR-0004 + `02-data-model.md`). No non-absorbed section contradicts a target doc.

## 9. Frontmatter validity — PASS

Every file under `docs/phases/` and `docs/blockers/`, plus `docs/03-roadmap.md`, has a parseable YAML frontmatter block with required keys present (phases: `id`/`title`/`status`/`depends_on`; blockers: `status` + full metadata; roadmap: `project_status`/`active_phase`).

Summary: 8 PASS, 1 FAIL

## Resolution (2026-06-03)

Check 5 (Glossary) FAIL addressed: the six terms (`FinderPredicate`,
`TopoEntity`, `FinderError`, `ConversationState`, `ConversationTurn`,
`PlanResult`) were added to the ubiquitous-language glossary in
`docs/02-classes.md`. All six now resolve. Audit clean → T5 greenlit.

Note (not a check failure): `docs/02-classes.md:84,339` still reference `face_id_at_capture`, which T5 Day-1 renames to `entity_id_at_capture` (`phase-T5.md:52`). This is a planned in-phase change, not a current contradiction, but the architecture doc will need updating during T5.

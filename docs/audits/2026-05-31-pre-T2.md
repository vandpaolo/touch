---
audit: pre-T2
date: 2026-05-31
result: 7 PASS, 2 FAIL → 9 PASS after fixes (see re-verification addendum)
gating: false   # neither FAIL gated a T2 start (both doc-quality); both since fixed
---

# Pre-T2 design audit — 2026-05-31

Independent Auditor sub-agent (no conversation context), 9 checks.

## 1. Coverage — PASS

Every functional requirement F1–F31 maps to at least one component/class in `docs/02-architecture.md` or `docs/02-classes.md`. Five F-IDs are covered by an owning component but carry **no explicit F-tag** in the architecture/classes docs (cosmetic, not orphans):

- **F8** (op appends to history + viewport updates) — covered by `document.append()` (`02-classes.md:97`), `Session._apply` (`02-classes.md:85`), `doc-store.applyEvent` (`02-classes.md:247`); not tagged.
- **F17** (cancel long-running op) — covered by `session` cancel token (`02-architecture.md:180`, `02-classes.md:82`), the Cancellation cross-cutting note (`02-architecture.md:342`), and the `web/prompt` cancel test (`02-classes.md:419`); not tagged.
- **F26** (GitHub-hosted, semver tags, CHANGELOG) — covered by the Distribution/Lifecycle context + `.github/workflows/release.yml` (`02-architecture.md:291`); not tagged.
- **F28** (download `.exe`, no CLI) — covered by N4 install row (`02-architecture.md:306`) + Distribution context; not tagged.
- **F30** (`out_root` → `/srv/touch/`) — covered by `config` (`02-architecture.md:190`, `02-classes.md:412`); not tagged.

No orphans.

## 2. NFR satisfaction — PASS

All twelve NFRs N1–N12 have a row in the "Non-functional requirements — design satisfaction" table (`docs/02-architecture.md:301-314`). No orphan N-IDs.

## 3. Reference integrity — FAIL

All cross-*doc* markdown links resolve. But 18 links into the **source tree** are broken — they point at the pre-rename `src/maquette/...` paths (the package was renamed to `src/touch_backend/` in T1a; confirmed `src/maquette` no longer exists). All are in closed Maquette-era phase **report** files plus one in `phase-T1a.md`:

- `phases/phase-0-report.md:21-22` → `../../src/maquette/{intent,intent_validation,pricing,config}.py`
- `phases/phase-1-report.md:22,49` → `../../src/maquette/adapters/{__init__,build123d_target}.py`
- `phases/phase-2a-report.md:23-25,55` → `../../src/maquette/agent/{sanity,planner,worker}.py`, `adapters/build123d_target.py`
- `phases/phase-2b-report.md:22-26` → `../../src/maquette/{adapters/build123d_target,render/orthographic,agent/executor,agent/loop}.py`
- `phases/phase-3-report.md:22` → `../../src/maquette/agent/loop.py`, `cli.py`
- `phases/phase-T1a.md:38` → `../../src/maquette/config.py`

None are in the target phase (`phase-T2.md` links are clean) or in the formal design docs, so this does not gate T2 — but the references are stale.

## 4. ADR coverage — PASS

The six "Decisions deferred" items (`docs/02-architecture.md:350-371`) each carry an explicit justification for remaining deferred — the section preamble (`:350`) states they become ADRs "when forced," and each item names its trigger (promote "if contested" #1, "if we ever need third-party tooling" #2, "if we ever expose on the LAN" #3, "until the UX demands it" #4, "if the file tree begs for multi-doc" #5, "fine for a solo dev" #6). No candidate lacks both an ADR and a justification.

## 5. Glossary — FAIL

Two domain terms used in `docs/02-classes.md` are missing from the ubiquitous-language table (`docs/02-classes.md:302-325`):

- **`Parameter`** — a value-object aggregate member (`02-classes.md:335`), a `TouchDocument` field (`02-classes.md:91`), and a full data-model entity (`02-data-model.md:139`), but has no glossary entry.
- **`ClarifyingQuestion`** — a class in the engine diagram (`02-classes.md:121`) and a `PlanResult` outcome; the parent concept "Conversation" is defined but the emitted-object term itself is not.

(Both were flagged in the pre-T1b audit and remain unfixed. Plumbing/implementation class names — `PlanResult`, `ConversationState`, `CancelToken`, `Tokens`, `KeychainBridge`, `Viewport`, etc. — are correctly out of scope for a domain glossary and are not counted, consistent with prior audits.)

## 6. Phase readiness (phase-T2) — PASS

`docs/phases/phase-T2.md` has all required slots non-empty: goal (`:15`), depends_on (`[T1b]`, frontmatter `:10` + section `:22`), minimum deliverable (`:37`), maximum deliverable (`:44`), a 10-row sprint table (`:51-62`), and exit criteria (`:64-71`). Note the Day-2 / R1 dependency on a `/pm-architecture` pre-pass to name `web/app` and add the capability shim — that pre-pass has already landed (both `web/app` and `web/platform` are present throughout `02-architecture.md`/`02-classes.md`), so the gate is satisfied.

## 7. Stale TODOs — PASS

No live `TBD`/`TODO`/`FIXME`/`<!-- gap:` markers in the formal design docs (`00-*`, `01-*`, `02-*`, `03-roadmap.md`, `adr/`). The only hits outside `notes/`+`audits/` are narrative references to the **already-resolved** executor process-model TBD (`phases/phase-T1b.md:43`, `phase-T1a-report.md:98`, `phase-T1b-report.md:52,96`) — `02-classes.md:349` now records that decision as "decided — decisions.md 2026-05-31 P3," and the matching entry exists at `notes/decisions.md:594`. No stale markers.

## 8. Notes hygiene — PASS

All `> absorbed into <doc> @ <date>` markers point at docs that exist and whose substance is present (spot-checked `constraints.md:16` → ADR-0004 content present; `inbox.md` absorption markers at `:151,355,624,886,1144,1346,1540,1589` target existing vision/requirements/architecture/data-model/ADR docs). No contradictions found between un-absorbed note sections and the target docs at the design-doc level.

## 9. Frontmatter validity — PASS

All phase-**plan** files (including `phase-T2.md`), all blocker files, and `03-roadmap.md` have valid YAML frontmatter with required keys present. Phase-**report** files use a distinct template keyed on `phase:` rather than `id:` (e.g. `phase-T1b-report.md`, `phase-0-report.md`) — they parse as valid YAML and are not plan files, so the plan-template required-key set does not apply. One minor cross-report inconsistency: `phase-T0-report.md` uses `min_goal_met`/`max_goal_met` while `phase-T1b-report.md` uses `min_met`/`max_met` — both valid YAML, no parse error.

---

Summary: 7 PASS, 2 FAIL

Neither FAIL gates a T2 start: check 3's broken links are all stale `src/maquette/` source paths inside closed report files (the target `phase-T2.md` links are clean), and check 5 is the two carried-over glossary entries (`Parameter`, `ClarifyingQuestion`). Both are doc-quality fixes.

---

## Re-verification addendum — 2026-05-31 (post-fix)

Per the user's decision to clear both FAILs before starting T2, the two
findings were fixed and re-verified by targeted check (not a fresh cold
re-audit — the other 7 checks were untouched by the fixes):

- **#3 Reference integrity → PASS.** Repointed all 15 broken markdown link
  *URLs* (`](../../src/maquette/…)` → `](../../src/touch_backend/…)`) across the
  6 flagged `docs/phases/` files (`phase-0/1/2a/2b/3-report.md`, `phase-T1a.md`);
  link *display text* left intact to preserve each report's historical record.
  All 13 distinct targets confirmed to exist under `src/touch_backend/`.
  `grep -rF '](../../src/maquette/' docs/phases/` now returns 0. One occurrence
  in `docs/audits/2026-05-18-pre-phase-2a-v2.md` was deliberately **not** touched
  (audit docs are append-only immutable history; it was not in the pre-T2 FAIL
  list). This overrides the pre-T1b audit's "leave historical links as-is" stance
  per the user's explicit choice.
- **#5 Glossary → PASS.** Added `Parameter` and `ClarifyingQuestion` entries to
  the ubiquitous-language table in `docs/02-classes.md` (definitions sourced from
  `02-data-model.md` and the `PlanResult`/F7 branch).

Post-fix result: **9 PASS, 0 FAIL.**

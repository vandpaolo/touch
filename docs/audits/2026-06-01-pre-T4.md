---
audit: pre-T4
date: 2026-06-01
result: 8 PASS, 1 FAIL
gating: false   # the single FAIL is the identical append-only-history residue from pre-T3
---

# Pre-T4 design audit — 2026-06-01

Independent Auditor sub-agent (no conversation context), 9 checks.

## 1. Coverage — PASS

Every F-ID F1–F31 maps to an owning component/class. T4's requirements:
- **F8** — `TouchDocument.append()` (`02-classes.md:97`), `Session._apply` (`:85`), `DocStore.applyEvent` (`:247`).
- **F9** — `web/history-ui`/`HistoryView` (`02-classes.md:41`), `DocStore.rewindTo` (`:248`), `Session._rebuild_from_history` (`:86`).
- **F10** — `touch_backend.document` load/save (`02-classes.md:18`, `02-architecture.md:181`), `web/file-tree` (`02-classes.md:42`).
- **F23** — `document` replay-from-history (`02-architecture.md:181`), `Session._rebuild_from_history` (`02-classes.md:86`).

No orphans.

## 2. NFR satisfaction — PASS

All N1–N12 have a design-satisfaction row (`02-architecture.md:301-314`); T4's N7 (`:309`) + N8 (`:310`) present.

## 3. Reference integrity — FAIL

All formal-design-doc links, all `docs/phases/` links, and `phase-T4.md` resolve (`phase-T4.md` references ADR-0006/0008 + `02-data-model.md` by name, no markdown links to break). The only broken links are the **identical, documented residue from pre-T3** — all in append-only history:
- `docs/notes/inbox.md:921,1133,1414` → `./02-intent-schema.md` (superseded Maquette doc name; no Touch equivalent)
- `docs/notes/inbox.md:1271` → `./03-agent-loop.md` (superseded Maquette doc name)
- `docs/audits/2026-05-17-pre-phase-1-v2.md:63` → `./02-classes.md` (wrong base; immutable audit file)

The pre-T3 fix held (`inbox.md:269` resolves). None gates T4.

## 4. ADR coverage — PASS

Six "Decisions deferred" (`02-architecture.md:350-371`) each carry a promote-when trigger; roadmap "Open decisions" mirror them + add #7. None lacks both an ADR and a justification.

## 5. Glossary — PASS

Ubiquitous-language table (`02-classes.md:302-327`) defines every T4-relevant domain term (Document/`.touch`, Operation, Selection, Finder, Solid, Mesh, Session, …). Plumbing identifiers out of scope.

## 6. Phase readiness — PASS

`phase-T4.md` has all slots non-empty: goal (`:15`), depends_on (`[T3]`), min (`:32`), max (`:56`), 11-row sprint table (`:64-74`), exit criteria (`:76-82`), risks (`:84-107`).

## 7. Stale TODOs — PASS

No live `TBD`/`TODO`/`FIXME`/`<!-- gap:` in formal design docs. The executor TBD is resolved (`02-classes.md:351` + `decisions.md:594`).

## 8. Notes hygiene — PASS

All `> absorbed into …` markers target existing docs; no contradictions with T4-relevant design docs.

## 9. Frontmatter validity — PASS

All `docs/phases/`, `docs/blockers/`, `03-roadmap.md` parse as valid YAML with required keys. `phase-T4.md` is `status: planned`; `active_phase: null` — consistent.

---

Summary: 8 PASS, 1 FAIL

The single FAIL (check 3) is non-gating: the six broken links are the identical, documented set from the pre-T3 audit — superseded Maquette doc names in the append-only migration seed (no Touch doc to repoint to) and one wrong-base link in an immutable historical audit file. None is in a live design doc or in `phase-T4.md`.

## Override — 2026-06-01

Started T4 over the single non-gating FAIL. Same basis as the pre-T3 override
(2026-06-01-pre-T3.md): the broken links are append-only-history residue
(`inbox.md` migration seed pointing at superseded Maquette doc names with no
Touch target; one immutable historical audit file). The one repointable link
was already fixed at pre-T3; nothing fixable remains. Not in any live design doc
or in `phase-T4.md`. T4 greenlit.

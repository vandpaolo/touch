# Pre-phase-2b audit — 2026-05-28

> Independent Auditor sub-agent review (no conversation context, docs read
> cold) run via `/pm-audit phase-2b` ahead of `/pm-phase-start`.

## 1. Coverage — PASS

Every functional requirement F1–F14 in `docs/01-requirements.md` maps to at least one component in `docs/02-architecture.md` § Layered responsibilities and/or a class/module in `docs/02-classes.md` § Module map:

- F1 → `cli`, `agent.loop` · F2 → `agent.planner` · F3 → `agent.worker` + `adapters.build123d_target` · F4 → `adapters.build123d_target` · F5 → `agent.executor` · F6 → `agent.sanity` · F7 → `render` · F8/F10/F11 → `agent.loop` · F9 → `agent.loop` + `pricing` · F12/F13/F14 → `cli` (+ `config` for F14, `agent.loop` for F13 exit-code mapping).

No orphan F-IDs.

## 2. NFR satisfaction — PASS

All ten NFRs (N1–N10) in `docs/01-requirements.md` have a row in the "Non-functional requirements — design satisfaction" table at `docs/02-architecture.md:199-210`. No orphan N-IDs.

## 3. Reference integrity — PASS

All cross-doc markdown links in the in-scope docs (`docs/02-*.md`, `docs/03-roadmap.md`, `docs/adr/*.md`, `docs/phases/*.md`) resolve to existing files. The one anchor link — `02-architecture.md:76` → `./02-classes.md#dependency-rules` — resolves to the `## Dependency rules` header at `02-classes.md:314`. `phase-0/1/2a-report.md` links into `../../src/...` and GitHub Actions URLs resolve to real repo paths/CI runs. No broken links.

## 4. ADR coverage — PASS

`docs/02-architecture.md` § Decisions deferred (lines 237-266) lists 6 items. Items 5 (prompt versioning) and 6 (sanity tolerance) are marked **Resolved** with ADR-0003 / ADR-0002 links. Items 1-4 (provider abstraction, conversational refinement, NX coverage, sandboxing strategy) each carry an explicit justification for remaining deferred ("no separate ADR yet", "v0.2", "explicit non-goal", "Will be an ADR if it does"). No candidate lacks both.

## 5. Glossary — FAIL

Domain terms appearing as class names in the `docs/02-classes.md` class diagrams have no row in the § Ubiquitous language glossary table:

- `Build123dTarget` — class diagram `02-classes.md:84`, module map row `02-classes.md:25`. Not in glossary.
- `NxOpenTarget` — referenced `02-classes.md:108`, module map row `02-classes.md:26`. Not in glossary.
- `Pricing` — class diagram `02-classes.md:214`. Module/infrastructure term; only `Tokens` and `ModelPrice` are glossed, not `Pricing` itself.
- `Config` — class diagram `02-classes.md:226`. Mentioned only parenthetically inside the `RunConfig` glossary entry (`02-classes.md:286`), no standalone row.

Note: `phase-2b.md:283-285` explicitly documents the `Build123dTarget`/`NxOpenTarget` glossary gap as a deferred pre-phase-5 `/pm-architecture` follow-up and instructs the audit not to block phase-2b on it (an override pattern carried from phase-2a). So while this check fails on the letter, the two adapter-leaf terms are a known, accepted deferral; `Pricing` and `Config` are not explicitly covered by that override.

## 6. Phase readiness — PASS

`docs/phases/phase-2b.md` has all required slots non-empty: Goal (lines 19-31), Depends-on (32-39), Minimum deliverable (99-178), Maximum deliverable (180-198), Sprint/day table with 5 rows (202-208), Exit criteria with 10 items (210-239). No empty slots.

## 7. Stale TODOs — PASS

A search for `TBD`, `TODO`, `FIXME`, and `<!-- gap:` across `docs/**/*.md` (excluding `notes/` and `audits/`) returned nothing. No stale markers.

## 8. Notes hygiene — FAIL

- `docs/notes/constraints.md:14-41` — the section "2026-05-28 — Adapter export contract (CF#1, confirmed live)" is marked `> absorbed into docs/02-data-model.md + docs/adr/0004 @ 2026-05-28` (line 16), but its substance **contradicts** what actually landed. The note proposes a uniform rule — "`body` is the conventional export variable... When `features` is non-empty, the last feature's id is `body` by convention" and "`_export` emits `export_step(body, "part.step")`" (lines 30-37). But `docs/adr/0004-build123d-export-variable.md:34-42` and `docs/02-data-model.md:240-258` explicitly **rejected** that uniform rule: feature-based Intents export `features[-1].id` (NOT forced to `body`), and "always export `body`" is listed as the rejected alternative (`0004:73-76`) precisely because it would break 5 snapshot fixtures. The absorbed note therefore misrepresents the decision it claims to be absorbed into. The note should be corrected (or re-marked) to reflect the shape-dependent split that was actually adopted.

Other absorbed sections (all in `docs/notes/inbox.md`, the 2026-05-16 migration seed) check out in substance against their target docs; the remaining notes files (`requirements.md`, `ideas.md`, `wishes.md`, `inspiration.md`) are empty templates with no content to contradict.

## 9. Frontmatter validity — PASS

All phase plan files (`phase-0.md`, `phase-1.md`, `phase-2a.md`, `phase-2b.md`) have a well-formed `---`…`---` block with all template keys (id, title, status, started, finished, min_goal_met, max_goal_met, blocker, depends_on, audit). Phase report files carry their own valid frontmatter (phase, status, min_met, max_met, duration_planned_days, duration_actual_days). `docs/03-roadmap.md` has valid frontmatter (project_status, active_phase). `docs/blockers/` contains only `README.md` (an index file, no frontmatter expected — no blocker docs exist). `phases/README.md` and `blockers/README.md` are index files without frontmatter, which is consistent with their role.

---

Summary: 7 PASS, 2 FAIL

The two FAILs:
- Check 5 (Glossary): `Build123dTarget`, `NxOpenTarget`, `Pricing`, `Config` lack glossary rows — though the two adapter-leaf terms are an explicitly documented, accepted deferral per `phase-2b.md:283-285`.
- Check 8 (Notes hygiene): the `constraints.md` ADR-0004 note is marked absorbed but its proposed uniform-`body` rule directly contradicts the shape-dependent rule actually adopted in ADR-0004 and `02-data-model.md`. This is the more substantive finding and worth correcting before phase-2b starts, since the note is the live record that disagrees with the spec the Day-1 export fix will implement.

## Resolution — 2026-05-28 (both FAILs cleared before start)

- **Check 8 fixed.** `docs/notes/constraints.md` § "Adapter export
  contract (CF#1)" rewritten to reflect the shape-dependent rule
  actually adopted (feature-based → `features[-1].id`; extras-only →
  `body`; degenerate → `AdapterRefusal`); the rejected uniform-`body`
  first draft is kept as marked history. The note no longer contradicts
  ADR-0004 / `02-data-model.md`.
- **Check 5 fixed.** Added the four missing glossary rows to
  `docs/02-classes.md` § Ubiquitous language: `Build123dTarget`,
  `NxOpenTarget`, `Pricing`, `Config`. The pre-phase-5 glossary
  close-out (`Dimension`/`DimensionMismatch` already closed in phase-2a)
  is now effectively complete for all flagged leaves — the recurring
  glossary FAIL hit at every prior `/pm-phase-start` should not recur.

**Post-resolution status: 9 PASS, 0 FAIL.** No override required;
greenlit cleanly.

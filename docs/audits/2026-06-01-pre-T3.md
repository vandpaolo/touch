---
audit: pre-T3
date: 2026-06-01
result: 8 PASS, 1 FAIL
gating: false   # the single FAIL is dead links in migrated/append-only history (notes/ + 1 audit), not in any live design doc or phase-T3
---

# Pre-T3 design audit — 2026-06-01

Independent Auditor sub-agent (no conversation context), 9 checks.

## 1. Coverage — PASS

Every functional requirement F1–F31 maps to at least one component/class in `docs/02-architecture.md` or `docs/02-classes.md`. Six F-IDs are covered by an owning component but carry **no explicit F-tag** in the design docs (cosmetic, not orphans):

- **F8** (op appends + viewport updates) — `document.append()` (`02-classes.md:97`), `Session._apply` (`02-classes.md:85`), `doc-store.applyEvent` (`02-classes.md:247`).
- **F17** (cancel) — `session` cancel token (`02-classes.md:17,82`), Cancellation cross-cutting note (`02-architecture.md:342`), `server`/`web/prompt` cancel tests (`02-classes.md:411,421`).
- **F21** (structured errors, no raw traceback) — `intent_validation.ContractViolation` (`02-classes.md:22`), `adapters.AdapterRefusal` (`02-classes.md:23,174`), `server` protocol dispatch.
- **F26** (GitHub host, semver, CHANGELOG) — Distribution/Lifecycle context + `release.yml` (`02-classes.md:289`, `02-architecture.md:291`).
- **F28** (download `.exe`, no CLI) — N4 install row (`02-architecture.md:306`) + Distribution context.
- **F30** (`out_root` → `/srv/touch/`) — `config` (`02-architecture.md:190`, `02-classes.md:414`).

No orphans.

## 2. NFR satisfaction — PASS

All twelve NFRs N1–N12 have a row in the "Non-functional requirements — design satisfaction" table (`docs/02-architecture.md:301-314`). No orphan N-IDs.

## 3. Reference integrity — FAIL

All cross-doc links in the **formal design docs** (`00-*`, `01-*`, `02-*`, `03-roadmap.md`, `adr/`) and all `docs/phases/` links resolve — including the target `phase-T3.md` (ADR-0008 and `notes/interaction.md` both present). The prior audit's two fixes hold: zero `](../../src/maquette/` links remain in `docs/phases/`, and all source-tree targets exist.

Six broken links remain, all inside `notes/` and `audits/` (migrated/append-only history, excluded from the prior audit's scope):

- `docs/notes/inbox.md:269` → `./adr/0001-intent-as-pivot.md` (wrong relative base — resolves to `notes/adr/...`; should be `../adr/`)
- `docs/notes/inbox.md:921,1133,1414` → `./02-intent-schema.md` (no such file; superseded Maquette doc name)
- `docs/notes/inbox.md:1271` → `./03-agent-loop.md` (no such file)
- `docs/audits/2026-05-17-pre-phase-1-v2.md:63` → `./02-classes.md` (resolves to `audits/02-classes.md`; should be `../02-classes.md`)

All in migrated/append-only history, not in any live design doc or the target phase — does not gate T3.

## 4. ADR coverage — PASS

The six "Decisions deferred" items (`docs/02-architecture.md:350-371`) each carry an explicit promote-when trigger. None lacks both an ADR and a justification.

## 5. Glossary — PASS

The ubiquitous-language table (`docs/02-classes.md:302-327`, 24 terms) covers every domain term, including the prior-audit additions `Parameter` (`:308`) and `ClarifyingQuestion` (`:315`). `Finder` defined (`:310`); `FinderPredicate` is its typed form. Plumbing/state-machine identifiers correctly out of scope.

## 6. Phase readiness (phase-T3) — PASS

`docs/phases/phase-T3.md` has all required slots non-empty: goal (`:15`), depends_on (`[T2]` `:10` + `:20`), minimum deliverable (`:34`), maximum deliverable (`:49`), an 11-row sprint table (`:58-70`), exit criteria (`:72-77`).

## 7. Stale TODOs — PASS

No live `TBD`/`TODO`/`FIXME`/`<!-- gap:` markers in the formal design docs. The only hits outside `notes/`+`audits/` are narrative references to the already-resolved executor process-model TBD, now recorded at `02-classes.md:351` + `decisions.md:593-594`.

## 8. Notes hygiene — PASS

All twelve `> absorbed into <doc> @ <date>` markers target existing docs whose substance is present (unchanged from the prior verified pass). Spot-checked `notes/interaction.md` — its 2026-06-01 mouse scheme is consistent with `phase-T3.md` and F3. **Soft tension:** that note says "no pan gesture specified… left unbound," while F3 lists shift+middle pan as a `must` — but the note flags pan as "revisit" and doesn't claim to override F3, so no design-doc contradiction. (Worth resolving via `/pm-requirements` if the no-pan scheme is permanent.)

## 9. Frontmatter validity — PASS

All phase-plan files (incl. `phase-T3.md`), both blocker files, and `docs/03-roadmap.md` parse as valid YAML with required keys. No parse errors, no missing keys.

---

Summary: 8 PASS, 1 FAIL

The single FAIL (check 3) is non-gating for a T3 start: all six broken links live in `docs/notes/inbox.md` (the migration seed) and one historical audit file — append-only history, not in any live design doc or `phase-T3.md`.

## Override — 2026-06-01

User chose "fix what's fixable, then start." Actions taken:
- **Fixed:** `docs/notes/inbox.md:269` — relative base `./adr/` → `../adr/`
  (target `docs/adr/0001-intent-as-pivot.md` confirmed present). Link now resolves.
- **Left intentionally (override):** the remaining 5 broken links —
  `inbox.md:921,1133,1414` → `./02-intent-schema.md` and `inbox.md:1271` →
  `./03-agent-loop.md` point at **superseded Maquette doc names with no Touch
  equivalent** to repoint to (the migration seed is append-only historical prose);
  and `docs/audits/2026-05-17-pre-phase-1-v2.md:63` is an **immutable audit file**
  (framework: audit docs are never overwritten). None gate T3.

Phase T3 greenlit on this basis.

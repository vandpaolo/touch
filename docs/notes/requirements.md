# Notes — Requirements

Things the system **must** do. "It needs to…", "the user wants…", "it should
support…". These feed directly into `01-requirements.md` (functional + NFR).

Long-form prose is fine. Don't worry about IDs or tables — `/pm-requirements`
will assign IDs and structure them.

---

